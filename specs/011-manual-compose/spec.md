# Manual Compose

**Status**: Rebaselined active slot placeholder - generate fresh Speckit artifacts before implementation.

**Branch**: `011-manual-compose`

## Purpose

Recreate manual envelope composition as a refreshed active branch after the earlier capture, retrieval, action, memory, and cloud-control contracts exist.

## Inputs To Preserve

- Deliberate composition and backfill concepts from archived `011-manual-compose`.
- Current intent set and duplicate-capture behavior from `016` and `017`.

## Stop Signs

- Manual compose must use the same seal, scrub, duplicate, and audit paths as passive capture.
- Backfill must remain wall-clock honest.
