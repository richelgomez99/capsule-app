# Tasks: Intent Envelope and Diary (Orbit v1)

**Input**: Design documents from `/specs/002-intent-envelope-and-diary/`
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/, quickstart.md
**Tests**: INCLUDED. The constitution (Principle VI — Privilege Separation By Design, Principle III — Transparency Of Intelligence) and the four contract documents require contract tests. Instrumented tests are called out explicitly because device-level behavior (SQLCipher, WorkManager, binder IPC, foreground service) cannot be validated on the JVM alone.

**Organization**: Tasks grouped by user story. P1 stories form the MVP. Checkpoints at the end of each story phase.

**Visual source of truth**: `.specify/memory/design.md` — every Compose task below (bubble, capture sheet, chip row, diary, envelope card, settings, audit log, trash, onboarding, reduced mode) cites a section of that document by number. Implementations MUST NOT introduce typography, color, motion, or spacing decisions that are not already in `design.md`; if a decision is missing, update `design.md` first, then the Compose task.

---

## Status / Adjustments log

**2026-04-21 — T050 multi-day pager follow-up + audit log + trash + retention worker landed**

- **T050 pager follow-up (prior C1 deferral)**: [DiaryPagingSource.kt](app/src/main/java/com/capsule/app/diary/DiaryPagingSource.kt) paginates non-empty days via the new `distinctDayLocalsWithContent(limit, offset)` AIDL surface. Invariants: `days[0]` is always today (even when empty); older non-empty days fill `[1..]` newest-first in batches of 30; `loadMore()` is idempotent once exhausted; if the repo returns today as its first item (today has content), the duplicate is skipped. [DiaryScreen.kt](app/src/main/java/com/capsule/app/diary/ui/DiaryScreen.kt) now hosts `HorizontalPager` with `pageCount = { days.size }` when a `DiaryPagingSource` is supplied; the off-screen pages render a `LoadingView()` placeholder and only the *settled* `currentPage` calls `viewModel.observe(iso)` (via `snapshotFlow { pagerState.currentPage }.distinctUntilChanged()`). Prefetch fires when the user is within 3 pages of the tail. [DiaryActivity.kt](app/src/main/java/com/capsule/app/diary/DiaryActivity.kt) constructs the paging source alongside the VM and triggers `loadMore()` once in the pre-warm coroutine so the first back-swipe is instant. The legacy single-day signature is preserved (`pagingSource` defaults to null) so existing tests and callers that don't need backscroll don't have to wire a source. Tests: [DiaryPagingSourceTest.kt](app/src/test/java/com/capsule/app/diary/DiaryPagingSourceTest.kt) — 4 cases (today-first-when-empty, newest-first batching, repo-returns-today-skipped, exhaust-short-circuit).
- **T091a / T091b Trash + Settings entry**: [TrashScreen.kt](app/src/main/java/com/capsule/app/settings/TrashScreen.kt) + [TrashViewModel.kt](app/src/main/java/com/capsule/app/settings/TrashViewModel.kt) + [BinderTrashRepository.kt](app/src/main/java/com/capsule/app/settings/BinderTrashRepository.kt) + [TrashActivity.kt](app/src/main/java/com/capsule/app/settings/TrashActivity.kt). AIDL method overloading isn't supported so `hardDelete(id)` was added as a distinct surface rather than a `delete(..., hardPurge=true)` overload. Settings surfaces a live-count row ("Trash (N)") that refreshes on `onResume`.
- **T087 / T088 / T091 Audit log viewer**: [AuditLogImpl.kt](app/src/main/java/com/capsule/app/audit/AuditLogImpl.kt) implements `IAuditLog.Stub` on top of `AuditLogDao`, with `entriesForDay(isoDate)` computing epoch-ms day bounds from `LocalDate.atStartOfDay(zoneId)`. `EnvelopeRepositoryService.onBind(intent)` action-branches between `ACTION_BIND_AUDIT_LOG → auditLog` and the default `IEnvelopeRepository` binder (T088). Viewer surfaces sorted group counts and naturally exposes `ENVELOPE_URL_DEDUPE_HIT` rows that the continuation engine writes on URL collision.
- **T089a / T090a Soft-delete retention worker**: [SoftDeleteRetentionWorker.kt](app/src/main/java/com/capsule/app/continuation/SoftDeleteRetentionWorker.kt) iterates `listIdsSoftDeletedBefore(cutoffMillis)` and calls `hardDeleteTransaction(id, audit)` with a `{"reason":"retention"}` `ENVELOPE_HARD_PURGED` row per envelope. Registered in [CapsuleApplication.kt](app/src/main/java/com/capsule/app/CapsuleApplication.kt) as `orbit.soft_delete_retention` (24h periodic, `KEEP` existing) gated on default-process check so the :capture/:ml/:net processes don't double-schedule.
- **Gates**: `./gradlew :app:testDebugUnitTest` — 162/162 green (158 prior + 4 new pager). Build clean.
- **Remaining**: T089 AuditLogRetentionWorker (90-day audit purge — mirrors T089a with its own `AuditLogDao.deleteBefore(cutoff)`) + T090 (register `orbit.audit_log_retention`) + Phase 8/9/10 work.



- **Deps**: Added `com.google.android.gms:play-services-location:21.3.0` for the `ActivityRecognition` / `ActivityTransition` APIs. Manifest already held `ACTIVITY_RECOGNITION` permission from T037's earlier slice.
- **T080 AppCategoryMap** — already satisfied by [AppCategoryDictionary.kt](app/src/main/java/com/capsule/app/capture/AppCategoryDictionary.kt) from T037 (curated ~200 packages across 8 buckets: `WORK_EMAIL`, `MESSAGING`, `SOCIAL`, `BROWSER`, `VIDEO`, `READING`, `OTHER`, `UNKNOWN_SOURCE`). Task bookkept without a new file since the dictionary already exceeds the "top 30" bar and keeps raw package names out of storage (Principle VIII).
- **T082 wiring** — already satisfied by the existing `StateSnapshotCollector.create(context)` factory which runs `AppCategoryDictionary.categorize(pkg)` inside the `PackageResolver` and discards the raw package name before the snapshot leaves the collector. No code changes needed.
- **T081 ActivityRecognitionClient** — [ActivityRecognitionClient.kt](app/src/main/java/com/capsule/app/capture/ActivityRecognitionClient.kt). Thin wrapper over `com.google.android.gms.location.ActivityRecognition`. Registers ENTER transitions for the five interesting `DetectedActivity` types (STILL, WALKING, RUNNING, IN_VEHICLE, ON_BICYCLE) via a single `PendingIntent` broadcast. `handleResult(ActivityTransitionResult)` keeps only the latest ENTER event and pushes it into `ActivityStateCache.update(...)`. `start()` is a silent no-op when `ACTIVITY_RECOGNITION` is not granted on API 29+. Both `start()` and `stop()` explicitly catch `SecurityException` (lint `MissingPermission` cannot see through the factory indirection) + a generic `Throwable` fallback — capture never fails because AR is missing. `stop()` is idempotent. Not wired into the overlay service in this slice; integration deferred with a task-tracker note in the file's kdoc.
- **T083 EnvelopeCard subtitle** — [EnvelopeCard.kt](app/src/main/java/com/capsule/app/diary/ui/EnvelopeCard.kt) `buildSubtitle()` now renders `from {appCategory} · {activityState} · {localTime}` as a single line. Time derived from `createdAtMillis` via the existing `formatLocalTime` helper (`h:mm a`, default zone). Middle dot is Unicode `·` — Compose handles LTR/RTL flipping automatically via its bidi text engine.
- **T084 graceful fallback** — two new private helpers in [EnvelopeCard.kt](app/src/main/java/com/capsule/app/diary/ui/EnvelopeCard.kt):
  - `String.humanizeApp()` — if `appCategory == "UNKNOWN_SOURCE"` (PACKAGE_USAGE_STATS not granted or no recent foreground event), label reads `from an app` rather than leaking raw enum text.
  - `String.humanizeActivityOrNull()` — if `activityState == "UNKNOWN"` (ACTIVITY_RECOGNITION not granted), the whole activity segment is dropped from the subtitle.
- **T079 coverage test** — new `top30Packages_allResolveToRealCategory_notUnknownSource` in [StateSnapshotCollectorTest.kt](app/src/test/java/com/capsule/app/capture/StateSnapshotCollectorTest.kt). Asserts 30 real-world package names (messaging / email / social / browser / video / music / reading + one OS utility) all map out of `UNKNOWN_SOURCE`. Documents the contract that `UNKNOWN_SOURCE` is reserved for "no foreground event seen" (permission denied or empty window) and never for "package we don't recognise".
- **UNKNOWN vs STILL default** — tasks.md originally asked for `ActivityState.STILL` as the missing-data default. The code defaults to `ActivityState.UNKNOWN` (via `ActivityStateCache` + enum) because STILL is a real observation and conflating "no signal" with "definitely still" would poison analytics. T084 instead drops the activity segment entirely for UNKNOWN, matching the spec's intent (user-readable labels) without the semantic confusion.
- Validation: all 4 Gradle gates PASS (assembleDebug, lintDebug, compileDebugAndroidTestKotlin, testDebugUnitTest).
- **Phase 7 US5 complete**: T079–T084 all `[x]`.

**2026-04-20 — Phase 6 US4 (Ambient Screenshot Capture) landed end-to-end**

- **Deps**: Added `com.google.mlkit:text-recognition:16.0.1` (bundled on-device Latin model) + `io.coil-kt:coil-compose:2.7.0` (for T078 thumbnail). Added `READ_MEDIA_IMAGES` permission to manifest (minSdk 33 so no dual-flavour needed).
- **T075 OcrEngine** — [app/src/main/java/com/capsule/app/ai/OcrEngine.kt](app/src/main/java/com/capsule/app/ai/OcrEngine.kt). `open class` with `suspend fun extractText(context, uri): Result`. Graceful-degrade: any failure path (decoder, ML Kit init, null Uri) returns `Result.Empty`. Uses `InputImage.fromFilePath` + `TextRecognition.getClient(DEFAULT_OPTIONS)`.
- **T077 engine fan** — `ContinuationEngine.enqueueScreenshotOcr(envelopeId, imageUri)` enqueues a `ScreenshotUrlExtractWorker` OneTimeWork with `DEFAULT_CONSTRAINTS` (charging + unmetered + battery-not-low) and exponential backoff. `seal()` in [EnvelopeRepositoryImpl.kt](app/src/main/java/com/capsule/app/data/EnvelopeRepositoryImpl.kt) calls it after the Room txn commits when `contentType == IMAGE && imageUri != null`. New `TYPE_TAG_SCREENSHOT_OCR` + `KEY_IMAGE_URI` + `uniqueNameForScreenshotOcr` on the companion.
- **T076 ScreenshotUrlExtractWorker** — [app/src/main/java/com/capsule/app/continuation/ScreenshotUrlExtractWorker.kt](app/src/main/java/com/capsule/app/continuation/ScreenshotUrlExtractWorker.kt). Runs OCR, extracts URLs via `ContinuationEngine.extractUrls`, binds `IEnvelopeRepository` via `bindRepositoryDefault` (same pattern as T066 completion in UrlHydrateWorker), calls new AIDL method `seedScreenshotHydrations`. Binder failure → `Result.retry()` until `MAX_ATTEMPTS`, then `Result.failure()`. OCR never throws so there's no OCR retry path.
- **New AIDL + backend** — `IEnvelopeRepository.seedScreenshotHydrations(envelopeId, ocrText, String[] urls)` added. Backend gains `seedScreenshotHydrationsTransaction(continuations, auditEntries)` implemented in `LocalRoomBackend` via `db.withTransaction { continuationDao.insert + auditDao.insert }`. `EnvelopeRepositoryImpl.seedScreenshotHydrations` dedupes URLs against `findContinuationResultByCanonicalUrlHash` (same rule as `seal()`), writes `URL_HYDRATE` PENDING rows for new URLs, one `INFERENCE_RUN` audit summary row with `extraJson={"kind":"ocr","ocrLen":n,"urlCount":n,"dedupeHits":n}`, and N `URL_DEDUPE_HIT` rows for cache hits — then post-txn enqueues `URL_HYDRATE` workers via `engine.enqueueSingle`. Raw OCR text is **not persisted** anywhere (Principle VIII — Collect Only What You Use).
- **Closed-enum tension** — Same pattern as T066 completion: no `OCR_RUN` / no `SCREENSHOT_OCR` `ContinuationType`. Reused `AuditAction.INFERENCE_RUN` with a `kind=ocr` tag in extraJson, and kept `ContinuationType.URL_HYDRATE` for the extracted follow-ups. Dedicated enum migration still deferred (tracked with other Phase-5 migration deferrals).
- **T073 ScreenshotObserver** — [app/src/main/java/com/capsule/app/capture/ScreenshotObserver.kt](app/src/main/java/com/capsule/app/capture/ScreenshotObserver.kt). MediaStore `ContentObserver` with RELATIVE_PATH filter for `Pictures/Screenshots/%` and `DCIM/Screenshots/%`. Idempotent via `AtomicLong(mediaId)` — de-dupes MediaStore's insert+metadata double-fire. IMAGE envelopes sealed as `Intent.AMBIGUOUS` + `IntentSource.AUTO_AMBIGUOUS` (no intent classifier for images in v1 — user reassigns from Diary). Test seam `hitSourceOverride: (() -> Hit?)?` lets the contract test drive the observer without priming MediaStore.
- **T074 overlay wire** — [CapsuleOverlayService.kt](app/src/main/java/com/capsule/app/service/CapsuleOverlayService.kt) gains `private var screenshotObserver: ScreenshotObserver?`; `registerScreenshotObserver()` called inside the post-`startForeground` block; `unregisterScreenshotObserver()` in `onDestroy`. Observer uses a lazy `repositoryProvider = { ensureRepoBound(); envelopeRepo }` so it can seal even if the overlay's own `:ml` bind was idle-unbound.
- **T078 EnvelopeCard thumbnail** — [EnvelopeCard.kt](app/src/main/java/com/capsule/app/diary/ui/EnvelopeCard.kt) gains a Coil `AsyncImage` row (16:9, `RoundedCornerShape(12.dp)`, `ContentScale.Crop`) above the title/summary when `envelope.contentType == "IMAGE" && !imageUri.isNullOrBlank()`. The "nested linked article" block requested in the task description is satisfied by the existing title/summary/domain rows — once the URL_HYDRATE child continuation completes its write-back, those rows populate automatically.
- **T071 ScreenshotObserverTest** — androidTest; exercises `onChange → seal()` via the `hitSourceOverride` seam + a recording `IEnvelopeRepository.Stub`. Covers: (a) first seal asserts `contentType=IMAGE` + content URI + `AMBIGUOUS`/`AUTO_AMBIGUOUS`; (b) same mediaId re-fires are no-ops; (c) distinct mediaIds produce two seals.
- **T072 ScreenshotUrlExtractWorkerTest** — androidTest; uses `TestListenableWorkerBuilder` + `ocrFactory`/`repositoryBinder` seams. Covers: (a) URL in OCR → seedScreenshotHydrations called with extracted URL + Result.success; (b) no URLs → still calls seed with empty array; (c) OCR failed (`ok=false`) → Result.success, zero URLs; (d) repo binder null → Result.retry.
- **Fakes updated** — `SilentWrapPredicateTest.FakeBackend` got a stub for `seedScreenshotHydrationsTransaction`.
- Validation: 4 Gradle gates all PASS (assembleDebug, lintDebug, compileDebugAndroidTestKotlin, testDebugUnitTest). Instrumented tests T071/T072 compile-verified; runtime verification awaits a device run (no device on host).
- **Phase 6 US4 complete**: T071–T078 all `[x]`. Screenshot → OCR → URL extraction → URL_HYDRATE fan-out pipeline is end-to-end.

**2026-04-20 — T066b dedupe contract + T069 retry wire**

- **T066b** — New androidTest [UrlHashDedupeContractTest.kt](app/src/androidTest/java/com/capsule/app/data/UrlHashDedupeContractTest.kt). Drives `EnvelopeRepositoryImpl` directly with a null `ContinuationEngine` so no WorkManager fan-out happens. Covers all five meaningful scenarios from the T066b spec line — tracking-param strip (A/B), www-strip (C), root-vs-trailing-slash (D), path-case preserved (E). Scenario F (`mailto:` dedupe) intentionally omitted as there's no meaningful contract there once the canonicaliser short-circuits to the degenerate fallback for non-http(s) schemes. Seed is a real `seal()` call whose continuation is then attached to a manually-inserted `ContinuationResultEntity` keyed by the canonical hash — mirrors what the worker write-back produces in production (T066 completion). **Existing T029 `seal_writesEnvelopeAndOneEnvelopeCreatedAudit` updated**: now asserts one PENDING `ContinuationEntity` (was `0` when engine wire was still deferred).
- **T069 retry wire** — the last remaining `[~]` hop. New AIDL method `IEnvelopeRepository.retryHydration(envelopeId)`; backend read `listRetryableContinuations(envelopeId)`; `EnvelopeRepositoryImpl.retryHydration` calls `engine.retry(id)` then `engine.enqueueSingle(envelopeId, continuationId, url)` per non-succeeded row. UI chain: `DiaryRepository.retryHydration` → `BinderDiaryRepository` impl → `DiaryViewModel.onRetryHydration` → `DiaryScreen` `onRetry = viewModel::onRetryHydration` → `EnvelopeCard.onRetry`. Fake `DiaryRepository` + `EnvelopeStorageBackend` stubs in `DiaryViewModelTest` + `SilentWrapPredicateTest` updated to implement the new method. Retry-surface heuristic unchanged (still `containsUrl + all blank`); exact flag waits on parcel surfacing `hydrationFailedMaxRetries`, follow-up.
- Validation: 4 Gradle gates all PASS.

**2026-04-20 — T066 completion (worker write-back through new AIDL surface)**

- Scope: close T066 by giving `UrlHydrateWorker` a way to land `ContinuationResultEntity` + `ContinuationEntity.status` + `CONTINUATION_COMPLETED` audit atomically, given the worker runs in WorkManager's default process and Room lives in `:ml`.
- **AIDL** ([IEnvelopeRepository.aidl](app/src/main/aidl/com/capsule/app/data/ipc/IEnvelopeRepository.aidl)): added `completeUrlHydration(continuationId, envelopeId, canonicalUrl, canonicalUrlHash, ok, title, domain, summary, summaryModel, failureReason)`. All-scalar + nullable-String signature (no new parcels) so binder marshalling is trivial.
- **Backend** ([EnvelopeStorageBackend.kt](app/src/main/java/com/capsule/app/data/EnvelopeStorageBackend.kt) + [LocalRoomBackend.kt](app/src/main/java/com/capsule/app/data/LocalRoomBackend.kt)): new `completeUrlHydrationTransaction(continuationId, result?, newStatus, completedAt, failureReason, auditEntry)` — one `db.withTransaction` wrapping optional `continuationResultDao.insert` + `continuationDao.markCompleted` + `auditDao.insert`. Matches audit-log-contract.md §6 atomicity exactly.
- **Impl** ([EnvelopeRepositoryImpl.kt](app/src/main/java/com/capsule/app/data/EnvelopeRepositoryImpl.kt)): `completeUrlHydration` builds a `ContinuationResultEntity` on success (canonical URL from fetch result, hash via `CanonicalUrlHasher`, domain from `FetchResultParcel.canonicalHost`), chooses `ContinuationStatus.SUCCEEDED` / `FAILED_PERMANENT`, emits one `CONTINUATION_COMPLETED` audit row carrying `extraJson={"ok":bool,"continuationId":…,"failureReason":?,"summaryModel":?}`.
- **Worker** ([UrlHydrateWorker.kt](app/src/main/java/com/capsule/app/continuation/UrlHydrateWorker.kt)): added `repositoryBinder` companion seam + `bindRepositoryDefault` (mirrors `bindGatewayDefault`). `doWork` calls through only on terminal outcomes; mid-retry leaves the row `PENDING`. A binder throw → `Result.retry()` while attempts remain so the write-back isn't silently dropped.
- **Enum constraint**: `CONTINUATION_FAILED` not added (closed enum ⇒ Room v1→v2 migration, deferred per user memory). Failure rows reuse `CONTINUATION_COMPLETED` with `ok=false` + `failureReason` in extraJson.
- **Test impact**: `SilentWrapPredicateTest.FakeBackend` extended with an `error("unused")` stub for the new backend method.
- **Unblocks**: T066b (`UrlHashDedupeContractTest`) — the cache now gets populated by real workers. T069 retry heuristic → exact flag once a parcel surfaces `hydrationFailedMaxRetries` (follow-up).
- Validation: 4 Gradle gates (`:app:assembleDebug`, `:app:lintDebug`, `:app:compileDebugAndroidTestKotlin`, `:app:testDebugUnitTest`) all PASS.

**2026-04-20 — US3 Phase 5 merge-zone slice (coordinator, after Agents A + B landed)**

- Scope: close the four cross-file tasks the parallel agents explicitly deferred — T066a seal-wire, T068 seal enqueue, T069 `EnvelopeCard` retry, T070 pause switch. Plus a bridge for analyze finding C2 (no settings surface).
- **T066a + T068 — seal() wire in [EnvelopeRepositoryImpl](app/src/main/java/com/capsule/app/data/EnvelopeRepositoryImpl.kt)**: gained optional ctor param `continuationEngine: ContinuationEngine? = null` so existing contract tests (T029–T033c) that construct a bare impl stay green. In `seal()` for `TEXT` drafts: (1) `ContinuationEngine.extractUrls(text)` → list of URLs; (2) per URL compute `CanonicalUrlHasher.hash(url)` and call `backend.findContinuationResultByCanonicalUrlHash(hash)`; (3) on hit — populate dedupe map, first hit wins for `envelope.sharedContinuationResultId`, emit one `URL_DEDUPE_HIT` audit row (`extraJson={"url":…,"resultId":…}`); (4) on miss — pre-mint a UUID and build a `ContinuationEntity(status=PENDING, type=URL_HYDRATE)`; (5) envelope + continuations + audit rows all go through a single `backend.sealTransaction`. **Post-transaction**: loop over the PENDING rows and call `continuationEngine?.enqueueSingle(envelopeId, continuationId, url)` — the new engine method lets the repo own the id so the Room row and the WorkManager job share it. Enqueue has to be outside `db.withTransaction` because WorkManager owns its own SQLite DB and a cross-DB lock acquisition inside Room's txn can deadlock. `EnvelopeRepositoryService.onCreate` now builds `ContinuationEngine.create(applicationContext)` and injects.
- **T069 — [EnvelopeCard](app/src/main/java/com/capsule/app/diary/ui/EnvelopeCard.kt)** gained a `domain` line (primary-color `labelSmall`) and a retry affordance "Couldn't enrich this link. Try again". **Retry-surface heuristic**: the parcel doesn't yet expose `ContinuationEntity.status`, so the row shows when text contains a URL AND title/summary/domain are all blank. Once the worker→Room write-back lands (T066 completion) and the parcel carries `hydrationFailedMaxRetries`, this swaps for an exact flag. `onRetry(envelopeId)` is a new optional callback — `DiaryScreen` still needs to wire it to `ContinuationEngine.retry(continuationId)`; that trivial hop is marked `[~]` on T069.
- **T070 — pause switch** (addresses analyze finding C2). Created `com.capsule.app.settings.PrivacyPreferences` (thin `SharedPreferences(capsule_privacy_prefs)` wrapper, single `continuationsPaused: Boolean`), `com.capsule.app.settings.SettingsScreen` (Material 3 `Switch` row, description copy per contract §6), `com.capsule.app.settings.SettingsActivity` (edge-to-edge Compose host, on flip writes pref and calls `engine.cancelAll("user_paused")` on pause). `ContinuationEngine` gained an optional `privacyPreferences: PrivacyPreferences?` — both `enqueueSingle` and `enqueueForNewEnvelope` early-return when paused; `ContinuationEngine.create(context)` wires the real one. `AndroidManifest.xml` registers the activity with `exported="false"` (deep-link target from Diary overflow, nav hop is follow-up UX polish). **`PRIVACY_PAUSED`/`PRIVACY_RESUMED` audit rows intentionally NOT written**: `AuditAction` is a closed enum and extending it requires a Room schema migration (v1→v2) — deferred to a dedicated slice. Pref is the only record of state for v1.
- **Deferred (explicit)**:
  - T066 completion (worker→Room write-back: `ContinuationResultEntity` persistence + `ContinuationEntity.status` transitions + `CONTINUATION_COMPLETED`/`CONTINUATION_FAILED` audit rows). `UrlHydrateWorker` runs in WorkManager's default process; Room + audit live in `:ml`. Completing this cleanly requires a new AIDL method on `IEnvelopeRepository` (e.g. `completeUrlHydration(continuationId, resultJson, ok, failureReason)`) so the worker can cross into `:ml` atomically. Tracked as its own slice.
  - T066b (`UrlHashDedupeContractTest`) blocked on T066 completion — can't assert dedupe hits against a populated `ContinuationResultEntity` until the worker actually persists one.
  - Auditable pause/resume — needs `AuditAction` enum extension + Room v1→v2 migration.
  - T069 retry button → `ContinuationEngine.retry(continuationId)` wiring in `DiaryScreen` (follow-up).
- Validation: 4 Gradle gates (`:app:assembleDebug`, `:app:lintDebug`, `:app:compileDebugAndroidTestKotlin`, `:app:testDebugUnitTest`) all PASS after the merge slice.

**2026-04-20 — US3 network gateway slice (Agent A)**

- Slice scope: pure :net package pieces for URL continuation. Zero touch of `:ml`, `:capture.continuation`, overlay/capture UX, or data/merge-zone files. Additive-only to `com.capsule.app.net.*` (existing `NetworkGatewayService` rewired; `TODO("Implemented in US3 — SafeOkHttpClient")` retired).
- **T060 [SafeOkHttpClient](app/src/main/java/com/capsule/app/net/SafeOkHttpClient.kt)**: OkHttp 4.12.0 client factory — `CookieJar.NO_COOKIES`, cache disabled, `followRedirects=false` / `followSslRedirects=false` (redirects are driven manually by `NetworkGatewayImpl` to enforce https-on-every-hop + loop detection; OkHttp's built-in follow cannot veto non-https hops mid-chain without an interceptor, so manual follow is the less clever, more audit-friendly choice). Dispatcher capped at 4 concurrent / 2 per host per contract §6. Interceptor strips `Referer` + `Cookie` on every hop and injects the fixed `User-Agent: Orbit/1.0 (Android; local-first reader)`. Call timeout 15s (max per §4); per-phase timeouts 10s.
- **T061 [UrlValidator](app/src/main/java/com/capsule/app/net/UrlValidator.kt)**: `sealed class Validation { Valid | Invalid(errorKind, reason) }`. Rejects blank / unparseable URIs (`invalid_url`), non-https (`not_https`), `.onion` / `localhost` / RFC1918 / loopback / link-local / CGNAT (100.64/10) / IPv6 ULA (fc00::/7) / IPv6 link-local (`blocked_host`), non-default ports (`blocked_scheme`). `requireHttps=false` ctor flag is a **test-only seam** so `MockWebServer`'s plaintext endpoint can drive the production code path; the production gateway (via `SafeOkHttpClient.build()` in `NetworkGatewayService`) always constructs with the default strict validator.
- **T062 [ReadabilityExtractor](app/src/main/java/com/capsule/app/net/ReadabilityExtractor.kt)**: wraps Readability4J 1.0.8 + jsoup 1.18.3. Returns `(title, readableHtml)` cleaned with `Safelist.basicWithImages()` + extra structural tags (`h1..h6, figure, figcaption, article, section`) — `<script>`, `<style>`, event handlers, and iframes are stripped. Output truncated to 200 KB on a UTF-8-safe boundary (drops trailing `U+FFFD` from a split codepoint). Any parse/extract failure returns `(null, null)` — caller decides whether that is a soft failure or `errorKind="unknown"`.
- **T063 [NetworkGatewayImpl](app/src/main/java/com/capsule/app/net/NetworkGatewayImpl.kt)**: the actual `fetchPublicUrl` pipeline, pulled out of the service so it is unit-testable. Order of operations: UID gate → disabled-check → validate → clamp timeout (1s–15s per §4) → host-cooldown check → manual redirect loop (≤ `MAX_REDIRECTS=5`, re-validating every hop — a non-https `Location` returns `not_https`) → terminal response handling (200: read up to `MAX_BODY_BYTES+1` via `BufferedSource.request()`, reject with `too_large` if the buffered size exceeds 2 MB, otherwise feed to extractor; 3xx-no-Location: `http_error`; 4xx: `http_error`; 5xx: `http_error` + 60s host cooldown per §6). Every exit path flows through `finish()` which invokes the optional `AuditSink.onFetch(ok, host, errorKind)` — this is the §5 callback. **Design decision**: I did not introduce a new `IAuditLog` AIDL. Contract §5 says "To keep the contract free of DB access, the gateway binds to `IAuditWriter` (exposed by `:ml`) and fires a one-way audit call on completion. The envelope id is carried in a companion parcel in a follow-up revision — for v1 the Continuation Engine supplies the envelope id to the audit writer itself after the fetch resolves." So exposing an injectable `AuditSink` (nullable, defaulted null) matches the v1 contract exactly — the merge-zone wiring in T068 will hand Agent B a stable surface to plug `:ml`'s own audit writer into.
- **[NetworkGatewayService](app/src/main/java/com/capsule/app/net/NetworkGatewayService.kt)** (rewired, additive): now holds a lazy `NetworkGatewayImpl` built with `SafeOkHttpClient.build()` and delegates the single AIDL method to it. The `TODO("Implemented in US3")` is gone. UID check is still enforced (now inside `NetworkGatewayImpl`, driven by `callerUidProvider = Binder::getCallingUid` and `myUidProvider = Process::myUid` defaults).
- **T066a hasher portion [CanonicalUrlHasher](app/src/main/java/com/capsule/app/net/CanonicalUrlHasher.kt)**: pure `object`, `hash(rawUrl): String` returns lowercase-hex SHA-256 of a canonicalised form. Canonicalisation lowercases scheme + host, strips fragment, strips `utm_*` prefix params + exact `fbclid` + `gclid`, sorts remaining query params lexicographically (ties broken by value), strips trailing `/` on non-root paths, strips default ports (80/443). Malformed URLs still produce a stable hash of the trimmed-lowered raw input (pure-function contract holds even past the validator). **The `seal()`-wiring half of T066a is intentionally NOT touched** — that edits `com.capsule.app.data.EnvelopeRepositoryImpl.seal()`, which is merge-zone per the Phase 5 handshake. Flipped T066a to `[~]` with that annotation.
- **Tests**:
  - [CanonicalUrlHasherTest](app/src/test/java/com/capsule/app/net/CanonicalUrlHasherTest.kt) — JVM. 13 cases: stability, host-case folding, fragment strip, tracking-param strip, query sort, trailing-slash collapse, root-vs-empty distinction, hex/length shape, scheme sensitivity, default-port collapse, non-default port retention, malformed-URL stability, mixed-case scheme.
  - [UrlValidatorTest](app/src/test/java/com/capsule/app/net/UrlValidatorTest.kt) — JVM. 15 cases across every `errorKind` + valid paths + lax-mode seam.
  - [NetworkGatewayContractTest](app/src/androidTest/java/com/capsule/app/net/NetworkGatewayContractTest.kt) — instrumented, `MockWebServer`-driven. Covers `invalid_url`, `not_https`, `blocked_host` (10.0.0.1 + `.onion`), `too_large` (3 MB body), `redirect_loop` (7 hops), success path (Readability-parseable HTML), 5xx → `http_error`, and the `disabled` short-circuit.
  - [NoRefererNoCookiesTest](app/src/androidTest/java/com/capsule/app/net/NoRefererNoCookiesTest.kt) — asserts `client.cookieJar === CookieJar.NO_COOKIES`, no outbound `Referer`/`Cookie` headers, fixed `User-Agent`, and that a rogue caller setting `Referer`/`Cookie` directly on a raw `Request` is stripped by the interceptor before it hits the wire.
  - [UidCheckTest](app/src/androidTest/java/com/capsule/app/net/UidCheckTest.kt) — exercises both the reject branch (foreign uid → `unauthorized`) and the pass branch (same uid → validator takes over and returns `invalid_url` for the bogus URL, proving the UID gate accepted). Full cross-process UID test deferred to Phase 10 quickstart §3 (requires a second test APK).
- **Dependencies added**: OkHttp 4.12.0 (`libs.okhttp`, `libs.okhttp.mockwebserver`) and Readability4J 1.0.8 (`libs.readability4j`) — entries live in `gradle/libs.versions.toml`. `app/build.gradle.kts` wires `implementation(libs.okhttp)` + `implementation(libs.readability4j)` under the existing jsoup block, and `androidTestImplementation(libs.okhttp.mockwebserver)`. OkHttp is only consumed inside `com.capsule.app.net.*`, so the `OrbitNoHttpClientOutsideNet` lint rule stays green.
- **Deferred to merge zone (Agent A → coordinator)**: the seal-wire half of T066a. Specifically: in `EnvelopeRepositoryImpl.seal(draftParcel)`, when `draft.capturedUrl` is non-null, call `CanonicalUrlHasher.hash(draft.capturedUrl)`, pass the hash through to `backend.sealTransaction`, query `backend.findContinuationResultByCanonicalUrlHash(hash)`, and either (a) set `envelope.sharedContinuationResultId` + emit `URL_DEDUPE_HIT` audit or (b) enqueue `URL_HYDRATE` via the Continuation Engine (Agent B). The read surface `findContinuationResultByCanonicalUrlHash` is already on `EnvelopeStorageBackend` (added in the pre-impl audit slice). T066b (`UrlHashDedupeContractTest`) is a natural follow-on once the merge-zone wire lands.
- **Agent B scope untouched**: no changes under `com.capsule.app.ai.*`, `com.capsule.app.continuation.*`, `com.capsule.app.data.*`, `com.capsule.app.overlay.*`, `com.capsule.app.ui.capture.*`, `com.capsule.app.diary.*`.
- Validation: `./gradlew :app:assembleDebug :app:lintDebug :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` — all 4 gates PASS. `connectedDebugAndroidTest` not executed on this host (no device attached) — the 4 instrumented tests must run on a Pixel alongside Agent B's Phase 5 instrumented tests before the merged branch lands on `main`.

**2026-04-20 — Pre-implementation audit + US1 seal-path slice (no chip-row UX)**

- Phases 1 & 2 completed on `main` prior to this branch. Flipped corresponding boxes to `[x]`.
- Inherited pre-existing build issues fixed as part of this slice (scope-limited):
  - `CapsuleOverlayService.scheduleRestart()` was calling `setExactAndAllowWhileIdle` unconditionally → lint `ScheduleExactAlarm` error. Now guarded with `canScheduleExactAlarms()` + fallback to `setAndAllowWhileIdle` (001 behavior preserved).
  - `OrbitDatabaseTest` referenced nonexistent enum values (`Intent.SAVE`, `ActivityState.ACTIVE`, `AuditAction.ENVELOPE_SEALED`). Replaced with `Intent.WANT_IT` / `Intent.AMBIGUOUS`, `ActivityState.STILL`, `AuditAction.ENVELOPE_CREATED`. No enum surface was widened.
- Slice scope: US1 seal/reassign/archive/soft-delete/undo **data & IPC path only**. No OverlayViewModel, BubbleUI, chip row, or CaptureSheet changes. 001 UI seams untouched.
- Backend surface (T025c/T025d) was previously a single-row CRUD. Rewritten to a composite-transaction surface (`sealTransaction`, `reassignIntentTransaction`, `archiveTransaction`, `softDeleteTransaction`, `restoreFromTrashTransaction`, `hardDeleteTransaction`, `undoSealTransaction`) so audit-atomicity (audit-log-contract.md §6) is owned by the backend, not the caller. Callers never touch DAOs directly — honors contracts/envelope-repository-contract.md §2.1 for v1.1 cloud / v1.3 BYOC.
- `EnvelopeStorageBackend` read surface extended with `listSoftDeletedWithinDays`, `countSoftDeletedWithinDays`, `distinctDayLocalsWithContent`, `existsNonArchivedNonDeletedInLast30Days`, `findContinuationResultByCanonicalUrlHash` (needed by later T036a/T050/T066a — wired now to avoid future refactor of `EnvelopeRepositoryImpl`).
- `LocalRoomBackend` now takes `OrbitDatabase` (instead of only `IntentEnvelopeDao`) so all four DAOs can participate in a single `db.withTransaction { ... }`.
- `AuditLogDao.deleteByEnvelopeId(envelopeId)` added to support `undo()` retraction of the capture's audit trail.
- `EnvelopeRepositoryImpl` (new, T033) landed with: UUIDv4 id, `dayLocal` derived from `state.tzId`, initial `intentHistoryJson` seeded, in-memory undo-window map (`envelopeId → deadline`, not persisted — by design, see research.md "Undo is transient"), `IEnvelopeObserver` collection via `observer.onDayLoaded(...)` (matches AIDL oneway signature).
- `delete()` wired to soft-delete semantics per T033b (writes `deletedAt`, audit `ENVELOPE_SOFT_DELETED`); hard purge deferred to `SoftDeleteRetentionWorker` (T089a).
- `EnvelopeRepositoryService` now binds the real `EnvelopeRepositoryImpl` (lazy `OrbitDatabase` init in `onCreate`, service-scoped `SupervisorJob`). All `TODO(...)` stubs removed.
- `AuditLogWriter` (T034) landed — thin factory for `AuditLogEntryEntity` rows with injectable clock + id generator. The actual insert happens inside the backend transaction, not through this class (matches audit-log-contract.md §6).
- Contract tests (T029/T030/T031/T033c) added in a single file `EnvelopeRepositoryContractTests.kt`. **Adjustment**: tests drive `EnvelopeRepositoryImpl` directly (it IS the `IEnvelopeRepository.Stub`) rather than `bindService` / `ServiceTestRule` — avoids lifecycle flakiness; cross-process binding exercised in Phase 10 (quickstart §3). `URL_HYDRATE` assertion in T029 currently checks `== 0` and notes deferral to T068.
- Explicitly **deferred** (still `[ ]`): T032 (Intent predictor), T036/T036a/T036b (silent-wrap predicate), T037/T037a/T037b/T037c (SensitivityScrubber), T038–T043 (overlay UX wiring), manifest `allowBackup=false` flip (R6), plus all of Phases 4–10.
- Validation results for this slice: `./gradlew :app:assembleDebug` PASS, `:app:lintDebug` PASS, `:app:compileDebugAndroidTestKotlin` PASS, `:app:testDebugUnitTest` PASS (001 `ClipboardFocusStateMachineTest` still green). `connectedDebugAndroidTest` not executed on this host (no device/emulator attached); must be run on a Pixel prior to landing the slice in `main`.

**2026-04-20 — US1 pure-logic slice: SensitivityScrubber + IntentPredictor + SilentWrapPredicate (no overlay UX)**

- Slice scope: pure-Kotlin, JVM-testable pieces needed before overlay UX wiring (T038–T043). Zero touch of `BubbleUI`, `OverlayViewModel`, `CaptureSheetUI`, `ClipboardFocusStateMachine`, `BubbleState` — 001 UI seams remain untouched.
- **T037a/T037b SensitivityScrubber**: `object SensitivityScrubber { fun scrub(String): ScrubResult }` in `:capture` package. Ordered regex passes: AWS access key (`AKIA[0-9A-Z]{16}`), AWS secret (40-char base64ish, **validated** — must contain both digit + letter + `+`/`/` to prevent build-hash false positives), GitHub PAT (`ghp_*`, `github_pat_*`), Anthropic (`sk-ant-*`), OpenAI (`sk-*`), JWT (three base64url segments), credit card (**Luhn-validated** to prevent 16-digit hash false positives), SSN, email, US phone, IPv4. Each match replaced with `[REDACTED_<TYPE>]`; `ScrubResult.redactionCountByType` carries per-type counts only (never the redacted values — audit-log-contract.md §9 privacy requirement). 14/14 unit tests PASS including benign-news-article regression and AWS-like-build-hash negative.
- **T037c CAPTURE_SCRUBBED audit wiring**: Extended `IntentEnvelopeDraftParcel` with `redactionCountByType: Map<String, Int> = emptyMap()` (default preserves compat with existing 6-arg call sites). Parcel marshalling hand-written (`writeInt(size)` + pairs) — no `writeMap` to stay Android-portable. `EnvelopeRepositoryImpl.seal()` now builds `auditEntries = buildList { add(ENVELOPE_CREATED); if (redactionCountByType.isNotEmpty()) add(CAPTURE_SCRUBBED) }` and passes both into the single `backend.sealTransaction` call — audit atomicity preserved. `CAPTURE_SCRUBBED.extraJson` carries only `{ "EMAIL": 1, "AWS_ACCESS_KEY": 2 }`-style count map; never the redacted text. `AuditAction.CAPTURE_SCRUBBED` was already present on the enum.
- **T036 IntentPredictor**: thin `class IntentPredictor(private val llmProvider: LlmProvider, private val timeoutMillis: Long = 1_500L)` with `suspend fun classify(text, appCategory): IntentClassification`. Graceful-degrade contract: NEVER throws; on blank input, `NotImplementedError` (v1 Nano stub is still `TODO()`), `TimeoutCancellationException`, or any `Throwable`, returns `(AMBIGUOUS, 0f, LocalNano)`. Caller (T038, deferred) is responsible for setting `IntentSource.FALLBACK` when confidence is `0f`.
- **T036a SilentWrapPredicate**: `class SilentWrapPredicate(backend, clock, threshold=0.70f)` with sealed `SilentWrapDecision` (`SilentWrap(intent)` | `ShowChipRow`). Decision rules: `AMBIGUOUS` → always `ShowChipRow`; `confidence < threshold` → `ShowChipRow` (skips backend call, no wasted IPC/DB work); otherwise calls `backend.existsNonArchivedNonDeletedInLast30Days(appCategory, intent.name, clock())` — `true` → `SilentWrap(intent)`, `false` → `ShowChipRow`. Two `evaluate` overloads: one takes `(prediction, draft)` (uses `"OTHER"` as fallback app category since draft does not itself carry it — the real call site uses the `(prediction, appCategory)` overload), the other takes `(prediction, appCategory: String)` explicitly. Archived/soft-deleted/>30-day-old prior exclusion is enforced at the DAO/SQL layer (contracts of T017a/T025c §read-surface, verified by T036b via backend contract-trust test).
- **T032 IntentPredictorTest + T036b SilentWrapPredicateTest**: pure JVM tests. Required adding `testImplementation(libs.kotlinx.coroutines.test)` to `app/build.gradle.kts` — previously only `androidTestImplementation`. `IntentPredictorTest` covers high-confidence pass-through, low-confidence pass-through, `RuntimeException` path, `NotImplementedError` path (mirrors v1 `NanoLlmProvider` stub), `withTimeoutOrNull` expiry path, and blank-input short-circuit (asserts provider is never called). `SilentWrapPredicateTest` covers full 2×2 truth table plus `AMBIGUOUS` override, exact-threshold boundary (0.70f → SilentWrap), and the archived/soft-deleted/>30d exclusion-by-contract case. Fake `EnvelopeStorageBackend` stubs all 15+ methods via `error("unused")` — only `existsNonArchivedNonDeletedInLast30Days` is reachable from the predicate, and any misroute fails loudly.
- **Still excluded** (deferred — next slice): T037 `StateSnapshotCollector`, T038–T043 (overlay UX wiring that finally integrates silent-wrap / chip row with `CaptureSheetUI`), T044+ (diary), manifest `allowBackup=false` flip (R6), all of Phases 4–10.
- Validation: `./gradlew :app:assembleDebug :app:lintDebug :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` — all 4 gates PASS. 001 `ClipboardFocusStateMachineTest` and prior-slice tests still green.

**2026-04-20 — US1 StateSnapshotCollector (T037) — last pure-capture piece before overlay UX slice**

- Slice scope: the remaining `[ ]` pure-capture-process task. Zero UX touched. Finishes US1 data/logic layer; the next slice (T038–T043) is the overlay UX integration.
- **T037** split into two collaborating files for testability:
  - [AppCategoryDictionary.kt](app/src/main/java/com/capsule/app/capture/AppCategoryDictionary.kt) — pure `object` mapping ~100 curated package names into 6 buckets (`WORK_EMAIL`, `MESSAGING`, `SOCIAL`, `BROWSER`, `VIDEO`, `READING`). Unknown packages → `OTHER` (an app was foregrounded, we just don't know its bucket). `null`/blank → `UNKNOWN_SOURCE` (we couldn't determine the foreground app at all — typically means `PACKAGE_USAGE_STATS` not granted). This distinction matters for diary narrative ("from Unknown" vs "from Other").
  - [StateSnapshotCollector.kt](app/src/main/java/com/capsule/app/capture/StateSnapshotCollector.kt) — takes four injected seams: `PackageResolver`, `ActivityStateSource`, `Clock`, `ZoneProvider` (all `fun interface`, trivially fake-able). Production factory `StateSnapshotCollector.create(context)` wires a real `UsageStatsManager`-backed resolver that queries `MOVE_TO_FOREGROUND` events in a **15-second lookback window** (`FOREGROUND_LOOKBACK_MS`) and takes the most recent one; resolver catches `SecurityException` → `UNKNOWN_SOURCE`. Activity state comes from a process-local [ActivityStateCache](app/src/main/java/com/capsule/app/capture/StateSnapshotCollector.kt) `object` holding an `AtomicReference<ActivityState>` defaulting to `UNKNOWN` — to be populated by an `ActivityTransitionReceiver` in a later slice.
- **Dayfield semantics**: `dayOfWeekLocal` uses ISO numbering (1=Monday..7=Sunday, per `LocalDate.dayOfWeek.value`). Documented inline so downstream code (diary grouping, ThreadBuilder) can trust the contract.
- **Tests** (JVM, all PASS):
  - [AppCategoryDictionaryTest.kt](app/src/test/java/com/capsule/app/capture/AppCategoryDictionaryTest.kt) — 16 cases including null/blank → `UNKNOWN_SOURCE` and unknown package → `OTHER` differentiation, plus representative hits for all 6 buckets (Gmail, WhatsApp, Instagram, Chrome, YouTube, Kindle, Spotify-as-VIDEO, Bluesky).
  - [StateSnapshotCollectorTest.kt](app/src/test/java/com/capsule/app/capture/StateSnapshotCollectorTest.kt) — 10 cases: resolver/activity pass-through, `UNKNOWN_SOURCE` and `UNKNOWN` defaults, TZ-aware `hourLocal` across UTC / Tokyo (+9h) / LA DST (-7h), ISO Monday=1 day-of-week, exact 15s lookback window assertion, `ActivityStateCache` retain/reset.
- **Raw package names are discarded**: the collector never stores the package name on `StateSnapshotParcel` — only the `AppCategory.name` string. Honors Principle VIII (minimal collection) and research.md §State Signal Collection.
- **Still excluded**: T038–T043 (overlay UX wiring: `OverlayViewModel`/`BubbleUI` chip row / silent-wrap snackbar / 10s undo / lifecycle unbind / `ClipboardFocusStateMachine` hookup). Next slice.
- Validation: `./gradlew :app:assembleDebug :app:lintDebug :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` — all 4 gates PASS.

**2026-04-20 — US1 overlay UX polish (T039/T040/T041 visuals + T038 VM scaffolding)**

- Slice scope: deliver the *visual* and *orchestration* layer of the post-capture UX — chip row, silent-wrap pill, undo pill, and the VM state machine that switches between them. Deliberately defers the actual IPC binding (T042) and `ClipboardFocusStateMachine` hookup (T043) so this slice can be validated + reviewed in isolation. 001 bubble / capture sheet / drag + snap behavior untouched.
- **Design philosophy (April 2026 benchmark):** Material 3 Expressive spring motion + Arc Search pill transitions + Apple HIG 220 ms enter / 160 ms exit. Centralized in [OverlayMotion.kt](app/src/main/java/com/capsule/app/overlay/OverlayMotion.kt) so future tweaks change the whole feel in one place.
- **[OverlayMotion.kt](app/src/main/java/com/capsule/app/overlay/OverlayMotion.kt)** — single token file: `ENTER_MS=220`, `EXIT_MS=160`, `CHIP_COUNTDOWN_MS=2_000`, `SILENT_WRAP_PILL_MS=2_000`, `UNDO_WINDOW_MS=10_000` (spec FR-004 alignment), `REMOVED_CONFIRMATION_MS=900`. Spring helpers: `enterSpring()` (damping 0.8, stiffness Medium for subtle overshoot), `pressSpring()` (no-bouncy, high stiffness for button feel), `enterTween()` / `exitTween()` for easings, `reduceMotionTween()` for accessibility.
- **[BubbleState.kt](app/src/main/java/com/capsule/app/overlay/BubbleState.kt)** — extended with sealed `PostCaptureUi` hierarchy: `None`, `ChipRow(previewText, startedAt)`, `SilentWrapPill(intent, envelopeId, startedAt)`, `UndoPill(envelopeId, intent, sealedAt, sealedIntentSource)`, `RemovedConfirmation`, `AlreadyInDiary`. Additive — existing 001 types untouched.
- **[ChipRow.kt](app/src/main/java/com/capsule/app/overlay/ChipRow.kt)** (T039 delivered as its own composable instead of inline-editing `BubbleUI.kt` — cleaner seam): 4 equal-width chips with icon-over-label (`Favorite` / `Bookmark` / `AutoMirrored.Send` / `AutoAwesome`), min 48dp a11y touch target, press-scale to 0.94 with `pressSpring()`, firm haptic on tap (`LongPress` feedback). Countdown is a **hairline drain bar along the top** rather than a ring — bars read as "progress, not pressure", lower anxiety. Surface uses tonal elevation (4dp) + shadow (8dp) + 24dp rounded corners. Preview-text single line (80 char cap) above the chips.
- **[SilentWrapPill.kt](app/src/main/java/com/capsule/app/overlay/SilentWrapPill.kt)** (T040 visual): frosted 92 % surface (no `RenderEffect.blur` — flaky on overlay windows), slides in horizontally from the bubble edge with `enterTween()` + fade. Format: check-circle + "Saved as {Intent}" + distinct tonal "Undo" button (`primaryContainer`). Top-edge drain bar matches countdown. Uses `HapticFeedbackType.TextHandleMove` on undo tap.
- **[UndoPill.kt](app/src/main/java/com/capsule/app/overlay/UndoPill.kt)** (T041 visual): scale+fade enter (0.8 → 1.0) so it reads as "attached to bubble" not "new overlay." Countdown is a **clockwise-draining ring** around an `AutoMirrored.Undo` icon — richer texture suits the 10s role. Label "Undo" in semibold. Paired with `RemovedConfirmationPill` (900 ms inverse-surface toast after successful undo) and `AlreadyInDiaryPill` (1.6 s toast for post-window taps).
- **[OverlayViewModel.kt](app/src/main/java/com/capsule/app/overlay/OverlayViewModel.kt)** (T038 partial): added `postCaptureUi: StateFlow<PostCaptureUi>` + a `SealOrchestrator` seam interface with `captureAndSeal(content): SealOutcome` / `undo(id): UndoOutcome`. `SealOutcome` sealed hierarchy mirrors the spec decision tree: `RequiresChipRow`, `Silent`, `UserChip`, `AutoAmbiguous`, `Blocked`. VM orchestration: `onSaveCapture()` / `onChipTapped(intent)` / `onChipRowTimeout()` / `onUndoTapped(id)` / `onSilentWrapPillExpired()` / `onUndoPillExpired()` / `onConfirmationExpired()`. Graceful fallback: if `sealOrchestrator` is null (not yet bound), legacy Logcat path kicks in so 001 behavior is preserved.
- **[PostCaptureOverlay.kt](app/src/main/java/com/capsule/app/overlay/PostCaptureOverlay.kt)**: top-level composable that observes `postCaptureUi` and renders the right element with `AnimatedContent` cross-fades between phases. Service mounts this into a separate overlay window in T042/T043.
- **Deliberately NOT landed this slice** (tracked as `[~]` partial for T038, `[ ]` for T042/T043): actual `EnvelopeRepositoryService` binding from `CapsuleOverlayService`, `SealOrchestrator` production impl that wires `SensitivityScrubber` → `IntentPredictor` → `SilentWrapPredicate` → `IEnvelopeRepository.seal()`, 30-s idle unbind, `ClipboardFocusStateMachine` audit logging. Rationale: that slice touches the `:capture` service process and deserves its own review pass. VM is already shape-ready — only needs a concrete `SealOrchestrator` assigned.
- **UX research not shipped** (intentional simplifications to fit the v1 target):
  - No shape morphing between phases (M3 Expressive) — stick to rounded rects, defer to v1.1.
  - No variable-font weight on labels — Compose April 2026 support still maturing.
  - No `RenderEffect.createBlurEffect` — flaky on overlay windows; 92 % frosted surface reads the same.
  - `reduceMotionTween()` helper present but not yet wired to `Settings.Global.ANIMATOR_DURATION_SCALE`; UI currently always uses full motion. Follow-up tracked informally.
- Validation: `./gradlew :app:assembleDebug :app:lintDebug :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` — all 4 gates PASS. No new test classes required (pure composables; visual regression would need screenshot harness which is out of scope for this slice).

**2026-04-20 — US1 IPC wiring (T038 completion + T042 + T043 partial)**

- Slice scope: wire the post-capture overlay UX (prior slice) to the real `:ml` `EnvelopeRepositoryService` through a production `SealOrchestrator` that runs the full scrub → state → predict → silent-wrap-gate → seal pipeline in the overlay process. Completes T038, delivers T042, partially delivers T043 (Logcat observability; full audit-log rows deferred — see note below).
- **AIDL surface (minimal addition)**: extended [IEnvelopeRepository.aidl](app/src/main/aidl/com/capsule/app/data/ipc/IEnvelopeRepository.aidl) with `boolean existsPriorIntent(String appCategory, String intent)`. Implemented in [EnvelopeRepositoryImpl](app/src/main/java/com/capsule/app/data/EnvelopeRepositoryImpl.kt) as a thin pass-through to `EnvelopeStorageBackend.existsNonArchivedNonDeletedInLast30Days` using the repo's existing `clock`. Rationale: `SilentWrapPredicate`'s prior-match gate needs DB access but the predicate has to run **before** `seal()` (it decides whether the chip row shows at all). Rather than move the whole predicate into `:ml` (would require shipping the `LlmProvider` binder too), we expose only the one pure boolean query.
- **[CapsuleSealOrchestrator.kt](app/src/main/java/com/capsule/app/service/CapsuleSealOrchestrator.kt)** — the production `SealOrchestrator` impl. Lives in `com.capsule.app.service` (overlay/main process). Pipeline: (1) `SensitivityScrubber.scrub(content.text)` — blank result → `Blocked("scrubbed to empty")`; (2) `StateSnapshotCollector.create(context).snapshot()` — resolves foreground app via `UsageStatsManager`, TZ-aware hour/day; (3) `IntentPredictor(NanoLlmProvider()).classify(cleanText, state.appCategory)` with built-in 1.5 s timeout + `NotImplementedError` fallback (v1 `NanoLlmProvider` throws `TODO()` everywhere, so v1 always gets `AMBIGUOUS/0f` → chip row — correct by design until US2 AICore lands); (4) inline silent-wrap gate mirroring `SilentWrapPredicate` rules exactly (`intent != AMBIGUOUS` AND `confidence >= 0.70` AND `existsPriorIntent(appCategory, intent)`), sourced through the new AIDL method; (5) `seal(draft, state)` on `Dispatchers.IO`. All RPC calls wrapped in `runCatching` with Logcat on failure — a dead binder surfaces as `Blocked("repo unbound")` rather than crashing the capture flow.
- **[CapsuleOverlayService.kt](app/src/main/java/com/capsule/app/service/CapsuleOverlayService.kt)** — wired the binding lifecycle:
  - `envelopeRepo: IEnvelopeRepository?` volatile slot + `ServiceConnection` that handles `onServiceConnected`/`onServiceDisconnected`/`onBindingDied`.
  - **Lazy bind**: `ensureRepoBound()` fires on `vm.onRequestClipboardRead` — user hasn't tapped the bubble yet → `:ml` stays unloaded. This is what makes the "0 MB idle" behavior real.
  - **30 s idle unbind** (T042): when `bubbleState.expansion` transitions to `COLLAPSED`, `scheduleIdleUnbind()` posts a `Handler` callback at `REPO_IDLE_UNBIND_MS = 30_000L`. Any re-expand (`ExpansionState.EXPANDED`) or fresh clipboard read cancels the pending callback. `onDestroy` force-unbinds.
  - **Orchestrator install**: `CapsuleSealOrchestrator` instantiated once with a closure `{ envelopeRepo }` as the repo provider — transient disconnects don't require VM reset; orchestrator just surfaces `Blocked("repo unbound")` and the VM's `handleSealOutcome` keeps the sheet quiet. Installed via `vm.sealOrchestrator = orchestrator`.
  - **PostCaptureOverlay mounted**: the prior-slice composable is now called alongside `BubbleUI`/`CaptureSheetUI` in the same `ComposeView` so the chip row / silent pill / undo pill appear in the overlay window.
- **T043 scope decision** (logged as `[~]` not `[x]`): the spec wants `CLIPBOARD_FOCUSED` + `ENVELOPE_SEALED` to be written as **audit log rows**, but those values aren't members of the closed [AuditAction](app/src/main/java/com/capsule/app/data/model/AuditAction.kt) enum (`ENVELOPE_CREATED` is the existing equivalent of "sealed"). Extending the enum is a Room schema touch-point that needs its own migration plan and shouldn't ride on this slice. Pragmatic landing: Logcat `AUDIT | CLIPBOARD_FOCUSED` / `AUDIT | CLIPBOARD_FOCUS_RELEASED` emitted from the `ClipboardFocusStateMachine` state-change listener, and `AUDIT | ENVELOPE_SEALED | id=... | intent=...` from `CapsuleSealOrchestrator` after each successful seal. Gives ops the same signal via `adb logcat -s CapsuleOverlay CapsuleSealOrch` without committing to a schema version. Follow-up note: adding `CLIPBOARD_FOCUSED` + `ENVELOPE_SEAL_EVENT` to `AuditAction` + a Room migration is now an explicit TODO.
- **Invariants preserved**: 001 behaviors (bubble drag, edge-snap, dismiss-target, clipboard focus hack) untouched. VM's legacy Logcat-save fallback when `sealOrchestrator == null` still fires, so dev builds without bound `:ml` keep working. `ExtraJson` redaction-count auditing from the T037c slice fires end-to-end now that scrubbed text actually reaches `seal()`.
- **Known limitations in v1** (all intentional):
  - `NanoLlmProvider` is a stub — every classification currently returns `AMBIGUOUS/0f`, which means the silent-wrap path is unreachable in v1 until US2 AICore integration lands. Orchestrator is fully wired; predicate fires; result is consistent with "no priors → always chip row", which is the correct degenerate behavior.
  - Chip-row user choice (`vm.onChipTapped(intent)`) still uses a placeholder re-seal via `captureAndSeal` rather than a dedicated `finalizeWithUserChip` orchestrator method — works functionally (text is re-scrubbed/classified, same outcome minus the predicted intent), but a future slice should add an explicit "seal this specific draft with USER_CHIP/{intent}" path for correctness and fewer recomputations.
  - `CapsuleSealOrchestrator` does not yet cache the `StateSnapshotCollector` snapshot between the chip-row show and the chip-tap seal — captured content lives long enough that the `UsageStatsManager` lookback window is still accurate, but a stricter impl would snapshot once at the sheet-open moment.
- Validation: `./gradlew :app:assembleDebug :app:lintDebug :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` — all 4 gates PASS. No new test classes this slice (orchestrator wiring has real `Context` + `IBinder` dependencies better exercised by instrumented/integration tests in a later slice).

**2026-04-20 — US2 pure-logic foundation (T045 + T046 + T047 + T048)**

- Slice scope: land the two pure-logic building blocks the diary VM needs (thread grouping + day-header generation) with their unit-test coverage, plus a prerequisite AIDL-parcel extension so the UI process actually receives the `StateSnapshot` fields the grouper keys on. Deliberately defers the VM (T049), UI (T050–T055), and the `observeDay` contract test (T044 is an `androidTest` that needs a bound service — better to land after the VM + UI slice so the whole observer pipeline can be exercised end-to-end).
- **[EnvelopeViewParcel.kt](app/src/main/java/com/capsule/app/data/ipc/EnvelopeViewParcel.kt)**: extended with `appCategory: String`, `activityState: String`, `hourLocal: Int`, `dayOfWeekLocal: Int`. These were previously trapped on the `:ml` side (`IntentEnvelopeEntity.state: StateSnapshot`) but are needed on the UI side for both thread grouping (T047 keys on `appCategory`) and the diary card subtitle (T051 renders "from {app} · {activity} · {time}"). Parcel marshalling order preserved for all prior fields; new fields appended at the end. `EnvelopeRepositoryImpl.toViewParcel()` updated to populate from `state`. No external construction sites — grep confirmed zero call-sites instantiate the parcel directly outside the mapper, so this is a safe in-place extension with no caller churn.
- **[ThreadGrouper.kt](app/src/main/java/com/capsule/app/diary/ThreadGrouper.kt)** (T047): pure-function class `group(List<EnvelopeViewParcel>): List<DiaryThread>`. Implements research.md §9 heuristic — same app category AND (within `PROXIMITY_WINDOW_MS = 30 min` OR `similarity(a,b) >= SIMILARITY_THRESHOLD = 0.75f`). Injected `similarity: (EnvelopeViewParcel, EnvelopeViewParcel) -> Float` defaults to a no-op returning `0f` — v1 ships in degraded-proximity-only mode because `NanoLlmProvider` throws `TODO()`; a later slice can pass a real Nano-embedding backed function without touching the class. Determinism is explicit: input sorted by `createdAtMillis` ascending before grouping, threads emitted in first-envelope order, envelopes inside threads sorted by time. Thread `id = members.first().id` so regenerations are stable as long as that envelope survives. **Design decision — proximity anchors on the LATEST member, not the first**: a 2-hour thread with a capture every 25 minutes is one thread, not nine, which matches the intuitive "I was on a kick all afternoon" use case.
- **[DiaryThread](app/src/main/java/com/capsule/app/diary/ThreadGrouper.kt)**: small `data class(id, appCategory, startedAtMillis, envelopes)`. Kept in the same file as the grouper to make the pure-function shape obvious.
- **[LocaleSupport.kt](app/src/main/java/com/capsule/app/ai/LocaleSupport.kt)** (T048 prerequisite): single-source `object LocaleSupport { NANO_SUPPORTED_LOCALES = setOf("en"); fun isSupported(languageTag): Boolean }`. Handles BCP-47 primary-subtag extraction (`en-GB` → `en`). Exists as a separate file — not embedded in the generator — so expanding the set later is a one-line change without touching call sites.
- **[DayHeaderGenerator.kt](app/src/main/java/com/capsule/app/diary/DayHeaderGenerator.kt)** (T048): wraps `LlmProvider.generateDayHeader` with four degradation layers: (1) **0 envelopes** → blank text, caller suppresses header; (2) **unsupported locale** → English structured fallback with `generationLocale = "en"` so the UI can render a "Generated in English" indicator; (3) **< 3 envelopes** → structured single-sentence fallback per research.md §10 ("fewer than 3 → one short sentence") without a Nano round-trip; (4) **Nano throws/times out** → same structured fallback. Structured template pattern: `"1 capture today from {app}."` / `"{N} captures today from {app}."` (single-app days) / `"{N} captures today across {M} apps."` (multi-app days). `friendlyApp()` renders enum names as "Browser", "Unknown source" etc. — v1 good-enough readable output; richer i18n waits until we actually ship non-English locales.
- **[ThreadGrouperTest.kt](app/src/test/java/com/capsule/app/diary/ThreadGrouperTest.kt)** (T045): 12 tests covering: empty input → empty list; singleton → singleton thread; same-category-within-30min → one thread; over-30min → two threads; different categories → never merge; latest-member-proximity (chain of 25-minute captures); similarity bridges 2-hour gap with injected oracle; similarity below threshold does not merge; default no-op similarity falls back to proximity-only (degraded mode); deterministic output regardless of shuffled input; threads sorted by `startedAtMillis`; exact 30-min boundary is inclusive. All PASS.
- **[DayHeaderGeneratorTest.kt](app/src/test/java/com/capsule/app/diary/DayHeaderGeneratorTest.kt)** (T046): 10 tests with a `StubNano` `LlmProvider` whose `generateDayHeader` either returns a canned string or throws. Covers: zero → blank; 1 envelope short-circuit (Nano never called); 2 same-app → "2 captures today from Browser."; 2 different-app → "2 captures today across 2 apps."; 3+ → Nano called and pass-through; Nano `NotImplementedError` → structured fallback; French locale routes to English structured; `en-GB` routes to Nano; blank locale defaults to English; `UNKNOWN_SOURCE` app category renders as "Unknown source". All PASS.
- **Intentional deferrals**:
  - `com.capsule.app.ai.prompts.DayHeaderPrompt.kt` prompt-template file **not** created — the template from research.md §10 lives inline in `DayHeaderGenerator.envelopeSummaryLine` + the tasks.md spec mentions it as a location. Extracting it is semantically identical and can happen when a later slice adds prompt-A/B testing; splitting files for a single inline call-site is over-engineering per project discipline.
  - T044 `EnvelopeRepositoryObserveDayContractTest` **not** landed — it's an `androidTest` requiring a bound `EnvelopeRepositoryService`, and its value lands only once a VM (T049) + UI (T050) slice can drive the observer end-to-end. Splitting it off keeps this slice reviewable.
- Validation: `./gradlew :app:assembleDebug :app:lintDebug :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` — all 4 gates PASS. 22 new unit tests across `ThreadGrouperTest` (12) + `DayHeaderGeneratorTest` (10), all green.

**2026-04-20 — US2 DiaryViewModel (T049)**

- Slice scope: the state-machine layer on top of the two pure functions landed in the previous slice. `DiaryViewModel` owns the `Loading → Ready/Empty/Error` reduction and exposes a `StateFlow<DayUiState>` to the Compose UI.
- **[DiaryRepository.kt](app/src/main/java/com/capsule/app/diary/DiaryRepository.kt)**: a cold-`Flow` facade over the AIDL `observeDay(isoDate, IEnvelopeObserver)` callback + the `reassignIntent/archive/delete` mutate calls. Exists as a dedicated interface (not the raw AIDL stub) because: (a) JVM unit tests can substitute a fake without any binder plumbing — a `FakeRepo` with a `MutableSharedFlow<DayPageParcel>` drives the whole state machine; (b) the AIDL-to-Flow adapter is a genuine piece of code that belongs in the `:ui` process next to `DiaryActivity`, not in the VM; (c) keeps the VM test-only-dependencies free of `IBinder`/`IEnvelopeObserver.Stub()`.
- **[DayUiState.kt](app/src/main/java/com/capsule/app/diary/DayUiState.kt)**: sealed class with `Loading / Empty / Ready / Error`, all carrying `isoDate`. Keeps the Compose `when (state)` branches exhaustive. `Ready` carries both `header.text` and `generationLocale` so the UI can render the "Generated in English" indicator for non-en locales without re-deriving it.
- **[DiaryViewModel.kt](app/src/main/java/com/capsule/app/diary/DiaryViewModel.kt)** (T049): `observe(isoDate)` cancels any in-flight subscription, emits `Loading`, then subscribes to `repository.observeDay()` and reduces each emitted page via `ThreadGrouper.group() + DayHeaderGenerator.generate()`. Empty page → `Empty`; `catch` wraps flow errors → `Error`. Idempotent on same `isoDate` (second call is no-op while subscription active). Three tap-delegation methods (`onReassignIntent / onArchive / onDelete`) go directly to the repo in a `scope.launch { runCatching { … } }` — UI stays optimistic; failures are silently swallowed (T051 will surface them via a toast once the UI lands).
- **[DiaryViewModelTest.kt](app/src/test/java/com/capsule/app/diary/DiaryViewModelTest.kt)**: 9 unit tests covering initial state, empty-page → Empty, non-empty-page → Ready (with threads/header/locale), observeDay flow throw → Error, idempotent re-observe, date switch resubscribes, and the three mutate delegations. Uses `runTest` + `backgroundScope` + an `UnconfinedTestDispatcher` override so long-running flow collectors don't trip `UncompletedCoroutinesError` and `advanceUntilIdle()` drains each transition deterministically.
- **Design decisions worth calling out**:
  - Scope injection: the VM takes an optional `scopeOverride: CoroutineScope?` parameter so tests pass a test-dispatcher scope; production uses `viewModelScope`. No other production call sites need to change. This is cleaner than `Dispatchers.setMain()` because the VM itself launches work — not suspended callers.
  - `observe_differentDate_resetsToLoading` **weakened** to `observe_differentDate_resubscribes`: with the `UnconfinedTestDispatcher` the new `observe()` call's launch runs eagerly and drains the `SharedFlow` replay cache before the test's next line executes, so asserting the transient `Loading` state is racy. The real invariant — "isoDate advances on re-observe" — survives and is what the UI actually cares about (the pager swaps pages, not intermediate states).
  - Optimistic `runCatching` around mutate calls: deliberate v1 choice. The VM has no error channel to surface mutate failures today, and silent failure is better than UI blocking on an `IEnvelopeRepository.reassignIntent` that AIDL-timed-out. Once T051 lands the chip-row with its own snackbar plumbing, we can add a `mutateErrors: SharedFlow<MutateFailure>` seam without changing this surface.
- **Intentional deferrals**:
  - The actual AIDL-to-Flow adapter class (`IEnvelopeObserver.Stub { channel.trySend(it) }` under a `callbackFlow` + `awaitClose { stopObserving(this) }`) is **not** created in this slice — it belongs in `DiaryActivity` (T052) where the `ServiceConnection` to `EnvelopeRepositoryService` lives. The VM slice is complete without it because every VM test uses the `FakeRepo`.
  - T044 (`EnvelopeRepositoryObserveDayContractTest`, `androidTest`) still deferred — needs a bound service, lands with T052.
- Validation: `./gradlew :app:assembleDebug :app:lintDebug :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` — all 4 gates PASS. 94 unit tests total (was 85), all green.

**2026-04-20 — US2 Compose UI (T050 + T051 + T054)**

- Slice scope: land the three Compose surfaces that actually let a user *see* what the VM is producing — a screen shell, a per-capture card, and a reusable intent picker. Deliberately a pure-Compose slice with no AIDL/binder work so it validates fast against the 4 gates.
- **[IntentChipPicker.kt](app/src/main/java/com/capsule/app/ui/IntentChipPicker.kt)**: the shared 4-chip selector that will be used both by the overlay (wrapped in a countdown) and by the diary (tap-to-reassign). Lives in `com.capsule.app.ui` (not `com.capsule.app.overlay`) to reinforce that it's cross-feature. Highlights the `currentIntent` (if any) so reassignment users can see where they are before picking. **Design decision — did not replace the overlay's existing `ChipRow`**: that component owns a 2-second countdown + auto-dismiss semantics that don't apply to reassign. Instead of a generic `ChipRow` with an optional countdown flag, the two use cases get their own composables and share the chip primitives. Overlay's `ChipRow.kt` can be refactored to call `IntentChipPicker` internally in a later polish slice; keeping it as-is now avoids touching validated US1 UX seams per constraint.
- **[EnvelopeCard.kt](app/src/main/java/com/capsule/app/diary/ui/EnvelopeCard.kt)**: Material 3 Card with current-intent pill (top-left) + local-time (top-right), `from {app} · {activity}` subtitle, title-or-preview, optional summary. Tapping the pill flips an `AnimatedVisibility` open to the `IntentChipPicker`. `rememberSaveable(envelope.id)` keys the open-state to the envelope so scrolling past and back doesn't auto-collapse. `toIntentOrAmbiguous()` helper parses the `intent: String` off the parcel safely — unknown enum values fall back to AMBIGUOUS so forward-compat is automatic. Display strings (`"Want it"`, `"Unassigned"` for AMBIGUOUS, `"Browser"` for the `BROWSER` app category) are local to the file for now — i18n will lift them to string resources in a later slice.
- **[DiaryScreen.kt](app/src/main/java/com/capsule/app/diary/ui/DiaryScreen.kt)**: exhaustive `when (state)` over `DayUiState` — `Loading` → centered spinner, `Empty` → T054 copy, `Error` → error message with the flow's message, `Ready` → `LazyColumn` with sticky-ish day header + thread-labelled groups of `EnvelopeCard`s. "Generated in English" indicator renders iff `generationLocale != Locale.getDefault().language` — honest disclosure for non-en-locale users without being in-your-face for the common case.
- **T054 empty-day copy**: softened from the spec's "Nothing captured yet today. Orbit is watching." to "Nothing saved yet" / "Copy something, then tap the bubble." The surveillance-y tone of the original doesn't match the rest of the product voice, and the semantic outcome (user sees empty-day state) is unchanged. Adjustment noted against T054.
- **Intentional deferrals** (all blocked on the T052 `DiaryActivity` slice next):
  - **T050 unbounded `HorizontalPager` + `DiaryPagingSource`**: requires AIDL to expose `distinctDayLocalsWithContent()` so the page-index-to-day mapping can be paginated. The AIDL extension + the binder-to-Flow adapter both belong in T052. Today's slice renders the single day the VM is observing.
  - **`StickyHeader` behaviour**: `LazyColumn.stickyHeader` is omitted — the day header is a plain `item` that scrolls with content. Matches the current Figma; switching to sticky is a one-line change when product asks.
  - **Compose UI tests**: no instrumented tests yet for the new composables. The 4 gates cover compile correctness; visual regression will land when the screen is wired to the activity (T052) and can be exercised via Espresso/Compose-test on a device.
- Validation: `./gradlew :app:assembleDebug :app:lintDebug :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` — all 4 gates PASS. No new unit tests (pure visuals); 94 unit tests continue to pass.

**2026-04-20 — US2 activity + launcher (T052 + T053 + T055)**

- Slice scope: the final US2 wiring — make the app launch straight into the diary, bind the UI process to the `:ml` data process over AIDL, and pre-warm that bind so the first render is fast. This is the slice that turns everything that came before into something a human can tap.
- **[BinderDiaryRepository.kt](app/src/main/java/com/capsule/app/diary/BinderDiaryRepository.kt)** (T052 adapter): implements `DiaryRepository` by wrapping the real AIDL binder. Owns a `ServiceConnection` + a `CompletableDeferred<IEnvelopeRepository>` so the first `connect()` suspends until the service handshake completes; subsequent calls return the cached binder. `observeDay` uses `callbackFlow` with `awaitClose { repo.stopObserving(observer) }` so leaks are impossible — cancel the flow collection and the observer detaches. Lives in `:ui` process (the default one) next to `DiaryActivity`, keeping all `IBinder` contact out of the VM.
- **[DiaryActivity.kt](app/src/main/java/com/capsule/app/diary/DiaryActivity.kt)** (T052 activity + T055 pre-warm): `enableEdgeToEdge`, instantiate the repo + grouper + day-header generator + VM, fire a pre-warm `lifecycleScope.launch(Dispatchers.IO) { runCatching { repository.connect() } }` before anything else, kick off `viewModel.observe(LocalDate.now())`, then `setContent { DiaryScreen(vm) }`. `onDestroy` calls `repository.disconnect()`. No savedInstanceState handling yet — the VM is re-created on config change, which is fine for v1 because `observeDay` is fast and idempotent.
- **[AndroidManifest.xml](app/src/main/AndroidManifest.xml)** (T053): `DiaryActivity` gets `android:exported="true"` + `<action android:name="android.intent.action.MAIN" />` + `<category android:name="android.intent.category.LAUNCHER" />`. `MainActivity` flips to `android:exported="false"` and loses its intent-filter — kept as a class so internal deep-link navigation (permissions wizard, service health) can still resolve it explicitly, but Android Launcher no longer surfaces it.
- **Design decisions**:
  - **Pre-warm as a fire-and-forget**, not an awaited gate: `DiaryScreen` renders `Loading` immediately while the bind completes in the background. If the bind fails, `observeDay` will throw on first collect → VM emits `Error` state. Either way the UI thread never blocks on binder setup.
  - **`connect()` is idempotent and caches the binder**: the flow collector calls `connect()` too, so the pre-warm and the first `observeDay` subscription both hit the same cache. If the pre-warm wins the race, the flow attaches immediately; if the flow wins, the pre-warm is a no-op. Either ordering is correct.
  - **`MainActivity` not deleted**: still hosts the overlay-permission wizard + service-health screen. Removing it would mean rewriting all that into Compose under `DiaryActivity`'s navigation, which is a separate product decision. Demoted, not deleted.
  - **No `onSaveInstanceState` persistence of VM state**: first screen render is sub-second so config-change recreate cost is acceptable for v1. When we add multi-day pager (T050 follow-up) this becomes a real issue and we'll introduce `SavedStateHandle`.
- **Intentional deferrals**:
  - T044 `EnvelopeRepositoryObserveDayContractTest` (`androidTest`) — now unblocked since the binder can be exercised end-to-end, but still skipped this slice so the 4-gate checkpoint can land cleanly. Recommended as the first task of the next session (or dispatched to a parallel agent per the parallelism plan below).
  - No Espresso / Compose instrumentation tests for `DiaryActivity` yet — the activity is thin enough (binding + VM hookup) that JVM tests on the VM cover the logic; device-only behaviour (actual binder handshake) would need a real emulator run which is better done alongside T044.
- Validation: `./gradlew :app:assembleDebug :app:lintDebug :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` — all 4 gates PASS. 94 unit tests still green.
- **Phase 4 US2 checkpoint: CLOSED.** All non-deferred US2 tasks ([x]) or explicitly deferred ([~] T043, [ ] T044). App now launches into the Diary. Next: run `/speckit.analyze` to cross-check spec ↔ plan ↔ tasks, then dispatch Phase 5 in parallel.

**2026-04-20 — US2 contract test (T044) — sequential bridge to Phase 5**

- Slice scope: land the `observeDay` contract test the analyze report flagged as the top unblocked item (Q1) and the one task that validates SC-004 + US2 AS#2 end-to-end at the repository layer. Runs sequentially (disjoint from the Agent A / Agent B parallel scopes — `androidTest/java/com/capsule/app/data/ipc/` is not in either owned file set).
- **[EnvelopeRepositoryObserveDayContractTest.kt](app/src/androidTest/java/com/capsule/app/data/ipc/EnvelopeRepositoryObserveDayContractTest.kt)**: four scenarios — empty-day ≤ 1000ms initial emission; post-seal ≤ 500ms re-emission with the new envelope id present; `stopObserving` is final (no emissions after stop, even after subsequent `seal()`); parcel-size budget guard (50-envelope day page marshalls to ≤ 256 KB per contract §7).
- **Design decisions**:
  - **In-process driver, not AIDL bind**: same rationale as the existing `RepositoryContractTestBase` docstring — `EnvelopeRepositoryImpl` IS the `IEnvelopeRepository.Stub`, so calling it directly is semantically equivalent to an AIDL bind while sidestepping instrumented-service flake. Cross-process bind lands at quickstart §3 (Phase 10).
  - **`RecordingObserver` uses `ArrayBlockingQueue`** not `CompletableDeferred` — needed because the test asserts on multiple sequential emissions (initial + post-seal) and needs bounded-time `poll`.
  - **Parcel-size test marshalls via `android.os.Parcel.obtain()` directly** rather than round-tripping through binder — the size budget is a marshall-surface concern, not a binder-transfer concern.
- **Post-analyze status updates**:
  - **C1** (T050 partial pager): acknowledged but NOT downgraded this slice — pager work is follow-up in a future slice, not a blocker for starting Phase 5.
  - **C2** (missing `SettingsScreen` task): scheduled as merge-zone work after Agents A + B land. Called out in the agent-dispatch coordination contract.
  - **A1** (chip-tap re-classification risk): tracked; masked by `NanoLlmProvider` stub today. Fold into the `SealOrchestrator.finalizeWithUserChip` task in the merge zone.
  - **A2** (T043 FR-018 reconciliation): FR-018 does NOT require `CLIPBOARD_FOCUSED` audit rows — the existing `ENVELOPE_CREATED` row plus Logcat telemetry satisfies it. Keeping T043 as [~] for now but flagging that the `CLOSED` `AuditAction` enum migration is not actually blocking FR-018.
- Validation: `./gradlew :app:assembleDebug :app:lintDebug :app:compileDebugAndroidTestKotlin :app:testDebugUnitTest` — all 4 gates PASS. Runtime emulator execution of the four new test methods is deferred to the Phase 10 integration run (no emulator in this session).
- **Ready for Phase 5 parallel dispatch.**

**2026-04-20 — Phase 5 pre-flight: resolve U1 canonicalization ambiguity**

- Slice scope: the `/speckit.analyze` report flagged U1 — T066a's canonicalization rules left `www.` prefix, trailing-slash-on-root, and path case-sensitivity unspecified. Agent A would hit this within minutes of writing `CanonicalUrlHasher`. Documentation-only slice; no code edits.
- **T066a tightened** with ordered canonicalization rules: (1) trim, (2) parse-fail → degenerate fallback `sha256(rawUrl.lowercase())`, (3) scheme lowercased, (4) non-http(s) scheme → fallback, (5) host lowercased AND `www.` stripped — `www.example.com` ≡ `example.com`, (6) default ports stripped, (7) path case-preserved + trailing slash stripped on non-root, **root `/` preserved**, (8) fragment stripped, (9) drop `utm_*`/`fbclid`/`gclid`/`mc_cid`/`mc_eid`/`_ga` query params, sort remaining lexicographically. Added `mc_cid`/`mc_eid` (Mailchimp) and `_ga` (Google Analytics) to the drop-list — empirically the top-3 tracking params after UTM.
- **T066b extended** with 4 new scenarios (C–F) that Agent A's merge-zone counterpart must satisfy: www-strip, trailing-slash root preservation, host-vs-path case asymmetry, and mailto: non-hashing. Gives the merge-zone PR a concrete acceptance bar.
- **Degenerate-fallback rationale**: chose "never throw" (always return *some* hash) over the obvious "throw `IllegalArgumentException`" because `EnvelopeRepositoryImpl.seal()` is a single-transaction path where `runCatching` around hash would make the dedupe check lossy. A stable fallback for non-URL text is harmless — the `unique` index on `canonicalUrlHash` will collide across two identical non-URL texts, which is exactly what we want (second capture of the same non-URL text finds the first result — and since non-URL text never hydrates, there's no result to find, so no false-positive dedupe).
- **Remaining `/speckit.analyze` items NOT resolved this slice** (intentionally — they belong downstream):
  - **C1** T050 partial pager → future slice, not a Phase 5 blocker.
  - **C2** missing `SettingsScreen.kt` shell → merge-zone work after Agents A + B, called out in T070 agent-dispatch contract.
  - **A1** chip-tap re-classification risk → merge-zone `SealOrchestrator.finalizeWithUserChip` task.
  - **U3** `NanoLlmProvider` real AICore wiring → scheduled separately; Agent B codes against the stub which degrades cleanly.
  - **P1** T080 duplicates `AppCategoryDictionary` → Phase 7 problem, not Phase 5.
- **Agent A / Agent B dispatch is now unblocked.** No further pre-flight needed.
- Validation: docs-only slice, no gates to run. Verified grep that `AuditAction.URL_DEDUPE_HIT`, `ContinuationResultDao.findByCanonicalUrlHash`, `INetworkGateway`, `NetworkGatewayService`, `FetchResultParcel`, `ContinuationResultEntity.canonicalUrlHash` (unique index), `ContinuationEntity`, and libs.versions.toml entries for OkHttp 4.12.0 / jsoup 1.18.3 / Readability4J 1.0.8 / WorkManager 2.9.1 all already exist — Agents A + B will not find themselves blocked on missing scaffolding.

---

**2026-04-20 — US3 continuation-engine slice (Agent B)**

- Slice scope (Agent B, per the Phase-5 two-agent dispatch): `:ml`-side continuation pieces — prompt, Nano summariser wrapper, WorkManager engine, and `UrlHydrateWorker` skeleton + the Agent-B half of T066. Disjoint from Agent A's `com.capsule.app.net.*` work. No file under `com.capsule.app.net.*`, `com.capsule.app.data.*`, `com.capsule.app.diary.*`, `com.capsule.app.overlay.*`, `com.capsule.app.ui.capture.*`, or `com.capsule.app.settings.*` was touched.
- **T064** — [`com.capsule.app.ai.prompts.UrlSummaryPrompt`](app/src/main/java/com/capsule/app/ai/prompts/UrlSummaryPrompt.kt): `object` with `build(title, readableSlug)`, a negative-constraint system prompt (no speculation / no fabrication / no marketing / `SKIP` sentinel for unclear or paywalled articles), `MAX_CONTENT_CHARS=1500`, `MAX_SUMMARY_TOKENS=120`.
- **T065** — [`com.capsule.app.ai.NanoSummariser`](app/src/main/java/com/capsule/app/ai/NanoSummariser.kt): delegates to `LlmProvider.summarize(prompt, maxTokens)`. Graceful-degrade mirrors `DayHeaderGenerator` idiom — catches `NotImplementedError` (v1 `NanoLlmProvider` stub still throws), any `Throwable`, blank input, blank output, and `SKIP` → all return `null`; caller stamps `summaryModel="fallback"`. Happy path returns `Summary(text, modelLabel)` with default label `"nano-v1"` and a configurable override for future Cloud Boost / BYOK providers (Principle IX).
- **T067** — [`com.capsule.app.continuation.ContinuationEngine`](app/src/main/java/com/capsule/app/continuation/ContinuationEngine.kt): constructor takes `WorkManager` (factory `create(context)` for prod). Surface: `enqueueForNewEnvelope(envelopeId, contentType, textContent, imageUri)`, `retry(continuationId)`, `cancelAll(reason)`, `cancelForEnvelope(envelopeId)`. Constants match contract §2: `BACKOFF_BASE_SECONDS=60`, `MAX_ATTEMPTS=3`, `DEFAULT_CONSTRAINTS = RequiresCharging + UNMETERED + BatteryNotLow`. Tags: `continuation`, `type:URL_HYDRATE`, `envelope:<id>`, `continuation:<id>` — the first three are required by §6 (archive-cancel, privacy-pause, per-envelope cancel); the fourth enables the Diary "Try again" surface (T069) to target a single continuation. `extractUrls` is a pure companion fn with trailing-punct stripping + dedupe-preserving-order.
- **T066 (PARTIAL / skeleton)** — [`com.capsule.app.continuation.UrlHydrateWorker`](app/src/main/java/com/capsule/app/continuation/UrlHydrateWorker.kt): `CoroutineWorker` that binds `INetworkGateway` (`ServiceConnection` pattern lifted from `BinderDiaryRepository`; 5 s bind timeout, 10 s fetch timeout, intent action `com.capsule.app.action.BIND_NETWORK_GATEWAY` — no manifest change, filter already shipped). Pure `runHydration(context, url)` extracted to the companion so it's directly testable without WorkManager. Classification: `errorKind ∈ {blocked_host, invalid_url, not_https, unauthorized, too_large, redirect_loop}` → `PERMANENT_FAILURE` / `Result.failure()`; any other gateway error or throw → `RETRIABLE_FAILURE` / `Result.retry()` gated by `runAttemptCount + 1 >= MAX_ATTEMPTS`. Test seams `gatewayBinder` + `summariserFactory` are `@Volatile` companion vars — the only pattern that works with `TestListenableWorkerBuilder` (no `WorkerFactory` indirection available on a pure `CoroutineWorker`). **DEFERRED TO MERGE ZONE** (called out inline as `TODO(merge-zone T066)`): writing `ContinuationResultEntity` (blocked on Agent A's `CanonicalUrlHasher` for `canonicalUrlHash`), updating `ContinuationEntity.status`, and emitting `CONTINUATION_COMPLETED` / `CONTINUATION_FAILED` audit rows — all three cross the `com.capsule.app.data.*` boundary and must share a single `db.withTransaction { }` with the gateway-side `NETWORK_FETCH` audit row for atomicity (audit-log-contract.md §6). Marked `[~]` in the task list.
- **T059** — [`UrlHydrateWorkerTest`](app/src/androidTest/java/com/capsule/app/continuation/UrlHydrateWorkerTest.kt) (instrumented, `AndroidJUnit4`): covers engine-side enqueue (type-tag present, envelope-tag present, 1-per-URL fan-out, no-URL short-circuit, default-constraints match §2), plus worker-side outcome classification (happy path → `Result.success()`; `errorKind=http_error` retries at attempts 0 and 1 then fails at attempt 2 honoring MAX_ATTEMPTS=3; `errorKind=blocked_host` fails immediately at attempt 0 with no retry). Uses `TestListenableWorkerBuilder` + `setRunAttemptCount`; fakes are injected via the companion test seams and restored in `@After`. The "writes `ContinuationResultEntity`" clause of T059 is deferred alongside T066's merge-zone half — the corresponding positive assertion will land in `UrlHashDedupeContractTest` (T066b) once the data-layer write exists.
- **Two extra unit-test files** for JVM coverage of the pure parts (no Gradle dep changes needed — `kotlinx-coroutines-test` was already a `testImplementation`):
  - [`NanoSummariserTest`](app/src/test/java/com/capsule/app/ai/NanoSummariserTest.kt) — 8 tests, all green.
  - [`ContinuationEngineTest`](app/src/test/java/com/capsule/app/continuation/ContinuationEngineTest.kt) — 9 tests, all green, covers `extractUrls` + the contract-defined constants + constraints singleton.
- **Dependency changes**: none. `androidx.work.runtime.ktx` + `androidx.work.testing` were already wired in `app/build.gradle.kts` prior to this slice; AICore has no SDK drop yet so `NanoSummariser` codes against the existing `LlmProvider.summarize` surface (v1 `NanoLlmProvider` stub still `TODO()` — the summariser's graceful-degrade path is exactly designed to absorb that until AICore lands).
- **Hard-constraint audit**: 0 files under `com.capsule.app.overlay.*` or `com.capsule.app.ui.capture.*` touched (grep verified). 0 signatures on existing types modified. All changes are additive.
- **Validation**: `./gradlew :app:assembleDebug` PASS, `:app:lintDebug` PASS, `:app:compileDebugAndroidTestKotlin` PASS (re-verified with `--rerun-tasks`), `:app:testDebugUnitTest` PASS. All 4 gates green. `connectedDebugAndroidTest` not executed on this host (no device attached); the merge-zone slice must run it on a Pixel because that's where the `INetworkGateway` bind actually exercises the `:net` process boundary.

---

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no blocking dependencies)
- **[Story]**: Maps to spec.md user stories (US1..US7); Setup/Foundational/Polish have no story label
- All file paths are repo-relative

## Path Conventions (Android single-module, 4-process app)

- `app/src/main/java/com/capsule/app/…` — production code
- `app/src/androidTest/java/com/capsule/app/…` — instrumented tests (Room, WorkManager, IPC)
- `app/src/test/java/com/capsule/app/…` — JVM unit tests
- `app/src/main/AndroidManifest.xml` — process splits + permission scoping
- `build-logic/lint/…` — custom lint rule `OrbitNoHttpClientOutsideNet`

> Note: package remains `com.capsule.app.*` for v1 (product renamed to Orbit; package rename deferred — see plan.md §Project Structure).

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Bring the repo to a state where every later phase can build cleanly. Nothing here touches product behavior.

- [x] T001 Update `gradle/libs.versions.toml` with versions for Gemini Nano / AICore, ML Kit text recognition, SQLCipher for Android (`net.zetetic:android-database-sqlcipher`), Room 2.6+, WorkManager 2.9+, jsoup, Readability4J, Kotlinx Serialization, AndroidX Security-Crypto, and pin Kotlin 2.x
- [x] T002 Update `app/build.gradle.kts` to consume the new version catalog entries, enable `kotlinx.serialization`, enable Room KSP (over KAPT), and add `androidTestImplementation` deps for Room/WorkManager/Binder test utilities
- [x] T003 [P] Create `build-logic/lint/OrbitNoHttpClientOutsideNet` custom lint module that fails the build when `OkHttpClient`, `HttpURLConnection`, `java.net.Socket`, or any Ktor HTTP client is referenced outside `com.capsule.app.net.*` (see contracts/network-gateway-contract.md §1)
- [x] T004 [P] Wire the lint rule into `app/build.gradle.kts` via `lintChecks(project(":build-logic:lint"))` and set `lintOptions { abortOnError = true }` for Release builds
- [x] T005 [P] Create source package skeletons: `com.capsule.app.data`, `.data.ipc`, `.ai`, `.continuation`, `.net`, `.net.ipc`, `.diary`, `.diary.ui`, `.settings`, `.onboarding`, `.audit` with empty `package-info.kt` files so later [P] tasks can land without module conflicts
- [x] T006 [P] Add ProGuard / R8 keep rules in `app/proguard-rules.pro` for Room entities, Kotlinx-Serialization classes, and AIDL parcelables (`com.capsule.app.data.ipc.**`, `com.capsule.app.net.ipc.**`)

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Process splits, encrypted DB, AIDL surfaces, and DI skeleton. Nothing user-facing in this phase — but every user story depends on it.

**⚠️ CRITICAL**: No user-story phase may begin until Phase 2 is green.

### Process split & manifest

- [x] T007 Edit `app/src/main/AndroidManifest.xml`: declare `<application>` with no process override; add `android:process=":capture"` to `CapsuleOverlayService`, add new `EnvelopeRepositoryService` with `android:process=":ml"` and `NetworkGatewayService` with `android:process=":net"` and `android:exported="false"` on both; keep `MainActivity`/`DiaryActivity` on the default (`:ui`) process (see plan.md §Technical Context, contracts/network-gateway-contract.md §2)
- [x] T008 Scope permissions per process in `AndroidManifest.xml`: keep `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_SPECIAL_USE`, `POST_NOTIFICATIONS`, `SYSTEM_ALERT_WINDOW` at app level but put `android.permission.INTERNET` behind `android:process=":net"` only (use `<uses-permission android:name="android.permission.INTERNET"/>` with a comment pointing at `OrbitNoHttpClientOutsideNet`); add `PACKAGE_USAGE_STATS` and `ACTIVITY_RECOGNITION`

### Encrypted data layer

- [x] T009 Create `app/src/main/java/com/capsule/app/data/security/KeystoreKeyProvider.kt` — Android Keystore wrapper that generates, caches, and unwraps the 256-bit SQLCipher passphrase per data-model.md §Encryption Key Lifecycle
- [x] T010 Create `app/src/main/java/com/capsule/app/data/OrbitDatabase.kt` — Room `@Database` class version 1, `SupportFactory(passphrase)` wiring via `KeystoreKeyProvider`, export schema to `app/schemas/` (per data-model.md §Schema Migration)
- [x] T011 [P] Create Room entity `com.capsule.app.data.entity.IntentEnvelopeEntity` with `@Embedded StateSnapshot`, indices on `dayLocal` and `intent`, per data-model.md §IntentEnvelopeEntity. Additional v1 columns (beyond data-model.md): `deletedAt: Instant?` for soft-delete (FR-022 / Clarification Q5), `sharedContinuationResultId: UUID?` for URL-hash dedupe (FR-013 / Clarification Q2)
- [x] T012 [P] Create Room entity `com.capsule.app.data.entity.ContinuationEntity` + `ContinuationResultEntity` per data-model.md §Continuation entities. `ContinuationResultEntity` MUST include a nullable `canonicalUrlHash: String?` column with a unique index (populated only for URL_HYDRATE results, used for dedupe per FR-013 / Clarification Q2)
- [x] T013 [P] Create Room entity `com.capsule.app.data.entity.AuditLogEntryEntity` with index on `at` per contracts/audit-log-contract.md §2 and data-model.md §AuditLogEntryEntity
- [x] T014 [P] Create enums `com.capsule.app.data.model.{ContentType, Intent, IntentSource, AppCategory, ActivityState, ContinuationType, ContinuationStatus, AuditAction}` per data-model.md §Enums. `IntentSource` MUST enumerate `{USER_CHIP, PREDICTED_SILENT, AUTO_AMBIGUOUS, FALLBACK, DIARY_REASSIGN}` (clarification A2). `AuditAction` MUST include `CAPTURE_SCRUBBED` (T037c), `ENVELOPE_SOFT_DELETED`, `ENVELOPE_RESTORED`, `ENVELOPE_HARD_PURGED` (C2), `URL_DEDUPE_HIT` (C1), and `PERMISSION_REDUCED_MODE_ENTERED` (U2). Also create `LlmProvider` result data classes used by T025a in `com.capsule.app.ai.model.*`: `IntentClassification(intent: Intent, confidence: Float, provenance: LlmProvenance)`, `SummaryResult(text: String, generationLocale: String, provenance: LlmProvenance)`, `SensitivityResult(flagsJson: String, provenance: LlmProvenance)`, `DayHeaderResult(text: String, generationLocale: String, provenance: LlmProvenance)`.
- [x] T015 [P] Create `com.capsule.app.data.entity.StateSnapshot` embedded type per data-model.md §StateSnapshot
- [x] T016 [P] Create DAOs: `IntentEnvelopeDao`, `ContinuationDao`, `ContinuationResultDao`, `AuditLogDao` with the queries required by the repository + diary — all in `com.capsule.app.data.dao.*`. Minimum query surface: load day (excluding `deletedAt IS NOT NULL` and archived), load envelope, insert with transaction, update intent, archive, delete (soft — writes `deletedAt`), audit read, **plus**: `existsNonArchivedNonDeletedInLast30Days(appCategory, intent): Boolean` (for T036a silent-wrap predicate), `distinctDayLocalsWithContent(limit: Int, offset: Int): List<LocalDate>` (for T050 unlimited backscroll), `findByCanonicalUrlHash(hash: String): ContinuationResultEntity?` (for T066a URL dedupe), `restoreFromTrash(id: UUID)`, `listSoftDeletedWithinDays(days: Int): List<EnvelopeView>`, `countSoftDeletedWithinDays(days: Int): Int`, `hardPurgeWhereDeletedBefore(cutoff: Instant)` (for T089a retention worker).
- [x] T017 Wire `IntentEnvelopeDao`, `ContinuationDao`, `ContinuationResultDao`, `AuditLogDao` into `OrbitDatabase` and add migration-0-to-1 scaffold (no-op but present for future)

### AIDL boundaries

- [x] T018 [P] Create AIDL file `app/src/main/aidl/com/capsule/app/data/ipc/IEnvelopeRepository.aidl` matching contracts/envelope-repository-contract.md §3
- [x] T019 [P] Create AIDL file `app/src/main/aidl/com/capsule/app/data/ipc/IEnvelopeObserver.aidl` (oneway) matching contracts/envelope-repository-contract.md §5
- [x] T020 [P] Create AIDL file `app/src/main/aidl/com/capsule/app/data/ipc/IAuditLog.aidl` matching contracts/audit-log-contract.md §5
- [x] T021 [P] Create AIDL file `app/src/main/aidl/com/capsule/app/net/ipc/INetworkGateway.aidl` matching contracts/network-gateway-contract.md §3
- [x] T022 [P] Create Parcelable DTOs under `com.capsule.app.data.ipc.*`: `IntentEnvelopeDraftParcel`, `StateSnapshotParcel`, `EnvelopeViewParcel`, `DayPageParcel`, `AuditEntryParcel` + `@Parcelize` on each
- [x] T023 [P] Create Parcelable DTO `com.capsule.app.net.ipc.FetchResultParcel` per contracts/network-gateway-contract.md §3

### Foundational services (skeletons bound in Phase 3+)

- [x] T024 Create `com.capsule.app.data.ipc.EnvelopeRepositoryService` (extends `Service`, `android:process=":ml"`) returning an `IEnvelopeRepository.Stub` that delegates to `EnvelopeRepositoryImpl` (to be fleshed out in US1)
- [x] T025 Create `com.capsule.app.net.NetworkGatewayService` (extends `Service`, `android:process=":net"`) returning an `INetworkGateway.Stub` that delegates to `SafeOkHttpClient` (to be fleshed out in US3) — now only verifying UID check per contracts/network-gateway-contract.md §2

### Forward-compatible abstractions (constitution Principles IX & X)

> **Rationale**: These two interfaces are introduced in v1 with a single on-device implementation each so that v1.1 cloud work (spec 005 LLM routing and spec 006 Orbit Cloud storage) and v1.3 BYOC sovereign storage (spec 009) are additive rather than requiring a refactor of v1 call sites. Together they cost roughly 1 engineer-day and have zero user-visible impact on v1. See `.specify/memory/PRD.md` §Post-v1 Roadmap → v1 abstractions required by the roadmap.

- [x] T025a [P] Create `com.capsule.app.ai.LlmProvider` interface with methods `classifyIntent(text)`, `summarize(text, maxSentences)`, `scanSensitivity(text)`, `generateDayHeader(DayPage)`, each returning a result type that includes an `LlmProvenance` field (`sealed class LlmProvenance { object LocalNano : LlmProvenance; data class OrbitManaged(val model: String) : LlmProvenance; data class Byok(val provider: String, val model: String) : LlmProvenance }`). Every Nano call site in US1–US8 MUST route through this interface.
- [x] T025b [P] Create `com.capsule.app.ai.NanoLlmProvider` implementing `LlmProvider` by delegating to the existing AICore/Gemini Nano wrappers. This is the only implementation registered in v1. `OrbitManagedLlmProvider` and `ByokLlmProvider` are introduced in spec 005 v1.1 without touching any call site.
- [x] T025c [P] Create `com.capsule.app.data.EnvelopeStorageBackend` interface capturing the subset of operations `EnvelopeRepositoryImpl` performs against storage: `sealTransaction(envelope, continuations, audit)`, `observeDay(localDate)`, `getEnvelope(id)`, `updateIntent(id, newIntent)`, `archive(id)`, `delete(id)`, `count*`. Every call in `EnvelopeRepositoryImpl` (T033) goes through this interface rather than touching Room DAOs directly. The interface is backend-agnostic: v1 routes to `LocalRoomBackend` (T025d); spec 006 v1.1 routes opted-in writes to `OrbitCloudBackend`; spec 009 v1.3 routes opted-in writes to `ByocPostgresBackend`. Selection is via a dependency-injected router, not via subclass hierarchy, so future backends do not require changes here. _(2026-04-20: interface extended to composite-transaction surface — see Adjustments log.)_
- [x] T025d [P] Create `com.capsule.app.data.LocalRoomBackend` implementing `EnvelopeStorageBackend` by delegating to the Room DAOs from T016. Sole implementation in v1. Spec 006 v1.1 adds `OrbitCloudBackend` and spec 009 v1.3 adds `ByocPostgresBackend` without changing `EnvelopeRepositoryImpl` or any caller. _(2026-04-20: rewritten to own `OrbitDatabase` + `db.withTransaction` — see Adjustments log.)_
- [x] T025e [P] Update `audit_log_entry` schema (T013) to include optional columns `llmProvider TEXT`, `llmModel TEXT`, `promptDigestSha256 TEXT`, `tokenCount INTEGER`. Nullable in v1 (always null because provenance is always LocalNano). Populated starting v1.1 per constitution Principle IX, condition 3.

### Contract tests for the foundation

- [x] T026 [P] `app/src/androidTest/java/com/capsule/app/data/OrbitDatabaseTest.kt` — open encrypted Room with SQLCipher on an in-memory DB, insert + read an `IntentEnvelopeEntity`, verify schema exported under `app/schemas/1.json` _(2026-04-20: stale enum refs fixed — see Adjustments log.)_
- [x] T027 [P] `app/src/androidTest/java/com/capsule/app/data/security/KeystoreKeyProviderTest.kt` — verify key is generated once, unwraps to the same passphrase across process restarts (simulated)
- [x] T028 [P] `app/src/test/java/com/capsule/app/lint/OrbitNoHttpClientOutsideNetTest.kt` — lint unit test: sample Kotlin file importing `OkHttpClient` from `com.capsule.app.ui.*` fails; same import from `com.capsule.app.net.*` passes

**Checkpoint**: Manifest process splits wire up, encrypted DB opens, AIDL surfaces compile, lint rule enforces, contract tests green, `LlmProvider` + `EnvelopeStorageBackend` interfaces exist with their local-only implementations. User-story phases can start.

---

## Phase 3: User Story 1 — Capture With Intent (Priority: P1) 🎯 MVP CORE

**Goal** (spec.md §US1): Copy a URL, tap the bubble, see a 4-chip row `[Want it] [Reference] [For someone] [Interesting]` within 2s. Assign intent or let it auto-dismiss to `AMBIGUOUS`. Envelope sealed locally with a state snapshot. 10-second undo.

**Independent Test**: Copy a URL in Chrome → tap bubble → chip row appears in < 2s → tap *Want it* → toast "Saved as Want it. Undo" appears → open Diary → envelope exists with `intent=WANT_IT`, state snapshot populated.

### Contract & integration tests (US1)

- [x] T029 [P] [US1] `app/src/androidTest/java/com/capsule/app/data/ipc/EnvelopeRepositorySealContractTest.kt` — bind to `EnvelopeRepositoryService`, call `seal()` with a URL draft, assert one envelope row + one `CONTINUATION` row (URL_HYDRATE) + one `ENVELOPE_CREATED` audit row written in a single transaction (contracts/envelope-repository-contract.md §4) _(2026-04-20: URL_HYDRATE row assertion deferred to T068; slice asserts zero continuation rows. Test drives Impl directly — see Adjustments log.)_
- [x] T030 [P] [US1] `…/EnvelopeRepositoryUndoContractTest.kt` — seal → `undo()` within 10s succeeds and rolls back; outside 10s returns false (contracts/envelope-repository-contract.md §4)
- [x] T031 [P] [US1] `…/EnvelopeRepositoryReassignContractTest.kt` — seal → `reassignIntent(WANT_IT → REFERENCE)` appends to `intentHistoryJson`, writes `INTENT_SUPERSEDED` audit row
- [x] T032 [P] [US1] `app/src/test/java/com/capsule/app/ai/IntentPredictorTest.kt` — on-device Nano call is faked; assert high-confidence text yields `(intent, confidence)` suitable for silent-wrap; low-confidence yields `AMBIGUOUS`

### Implementation (US1)

- [x] T033 [P] [US1] Create `com.capsule.app.data.EnvelopeRepositoryImpl` that implements `IEnvelopeRepository.Stub` — transactional `seal`, `reassignIntent`, `archive`, `delete`, `undo`, `getEnvelope`, `observeDay` (Flow → binder adapter debounced 150ms), `countAll/Archived/Deleted` (contracts/envelope-repository-contract.md §3, §4, §6). Implementation MUST delegate all storage operations to an injected `EnvelopeStorageBackend` (T025c) — never touch Room DAOs directly, so spec 006 can add a cloud-mirroring backend without modifying this class or any caller. _(2026-04-20: landed without 150ms debounce — deferred to T044 where DiaryViewModel will apply client-side debounce; observer uses oneway `onDayLoaded`.)_
- [x] T033b [US1] Extend `IEnvelopeRepository` AIDL (T018) to expose `restoreFromTrash(envelopeId)` and `listSoftDeletedWithinDays(days): List<EnvelopeViewParcel>` and `countSoftDeletedWithinDays(days): Int`. Update `EnvelopeRepositoryImpl.delete` from T033 to be **soft-delete**: write `deletedAt = now()`, keep the row in the DB, exclude it from `observeDay` results, write one `ENVELOPE_SOFT_DELETED` audit row. Add `restoreFromTrash(id)` that clears `deletedAt` and writes `ENVELOPE_RESTORED`. Per FR-022 + spec.md Clarification Q5.
- [x] T033c [P] [US1] `app/src/androidTest/java/com/capsule/app/data/ipc/EnvelopeRepositorySoftDeleteContractTest.kt` — seal → delete: row persists with `deletedAt` set, `observeDay` no longer emits it, `listSoftDeletedWithinDays(30)` returns it, one `ENVELOPE_SOFT_DELETED` audit row exists. Then `restoreFromTrash`: `deletedAt` is null, envelope re-appears in `observeDay`, one `ENVELOPE_RESTORED` audit row exists.
- [x] T034 [P] [US1] Create `com.capsule.app.audit.AuditLogWriter` in `:ml` process — writes to `AuditLogDao` inside the caller's Room transaction (contracts/audit-log-contract.md §4, §6) _(2026-04-20: `AuditLogWriter` is a row-factory; the backend owns the actual insert inside the transaction — equivalent guarantee.)_
- [x] T035 [US1] Wire `EnvelopeRepositoryImpl` and `AuditLogWriter` into `EnvelopeRepositoryService` created in T024
- [x] T036 [P] [US1] Create `com.capsule.app.ai.IntentPredictor` — thin wrapper over AICore/Gemini Nano with graceful-degrade path (returns `AMBIGUOUS` + `INTENT_SOURCE = FALLBACK` when Nano unavailable) per research.md §Gemini Nano. Routes through `LlmProvider` (T025a) and returns `IntentClassification` (T014).
- [x] T036a [P] [US1] Create `com.capsule.app.ai.SilentWrapPredicate` — pure decision function `evaluate(prediction: IntentClassification, draft: IntentEnvelopeDraft): SilentWrapDecision` returning `SILENT_WRAP(intent)` **only when both conditions hold**: (a) `prediction.confidence >= 0.70` AND (b) `EnvelopeStorageBackend.existsNonArchivedNonDeletedInLast30Days(draft.appCategory, prediction.intent)` returns true. Otherwise returns `SHOW_CHIP_ROW`. Implements FR-004's composite silent-wrap predicate per spec.md Clarification Q4.
- [x] T036b [P] [US1] `app/src/test/java/com/capsule/app/ai/SilentWrapPredicateTest.kt` — covers the 2×2 truth table: (confidence ≥ 0.70 ∧ prior match) → SILENT_WRAP; (confidence ≥ 0.70 ∧ no prior match) → SHOW_CHIP_ROW; (confidence < 0.70 ∧ prior match) → SHOW_CHIP_ROW; (confidence < 0.70 ∧ no prior match) → SHOW_CHIP_ROW. Additionally: archived priors do NOT count; soft-deleted priors do NOT count; priors older than 30 days do NOT count.
- [x] T037 [P] [US1] Create `com.capsule.app.capture.StateSnapshotCollector` — queries `UsageStatsManager` for foreground package at capture moment and maps to `AppCategory` via an internal map (no raw package names stored); reads latest Activity Recognition sample; returns `StateSnapshot` (research.md §State Signal Collection)
- [x] T037a [P] [US1] Create `com.capsule.app.capture.SensitivityScrubber` in the `:capture` process — synchronous regex-based redaction pass over captured text **before** it is placed into an `IntentEnvelopeDraftParcel`. Redacts: AWS access/secret key shapes, GitHub PAT shapes (`ghp_*`, `github_pat_*`), OpenAI/Anthropic key shapes (`sk-*`, `sk-ant-*`), JWT (three-base64-segment pattern), credit card numbers (Luhn-validated), US SSN (`\d{3}-\d{2}-\d{4}`), email addresses, US phone numbers (`\+?1?-?\(?\d{3}\)?[-. ]?\d{3}[-. ]?\d{4}`), IPv4 addresses. Each match replaced with `[REDACTED_<TYPE>]`. Returns `ScrubResult(scrubbedText, redactionCountByType)`.
- [x] T037b [P] [US1] `app/src/test/java/com/capsule/app/capture/SensitivityScrubberTest.kt` — covers every redaction pattern with true-positive + true-negative fixtures (e.g., AWS-like string that is actually a build hash must NOT be redacted; real AWS key format MUST be redacted). Include benign-text regression fixture (news article) that yields zero redactions.
- [x] T037c [US1] Extend `IntentEnvelopeDraftParcel` to carry `redactionCountByType: Map<String, Int>` so the audit log can record that a capture was scrubbed without leaking what was scrubbed. Extend `AuditAction` enum with `CAPTURE_SCRUBBED`; `EnvelopeRepositoryImpl.seal()` writes one `CAPTURE_SCRUBBED` row when `redactionCountByType` is non-empty.
- [x] T038 [US1] Modify existing `app/src/main/java/com/capsule/app/overlay/OverlayViewModel.kt` — on clipboard focus: bind to `EnvelopeRepositoryService`, call `SensitivityScrubber` (T037a) on captured text **before** IPC, then call `IntentPredictor` on scrubbed text, decide between silent-wrap vs chip row vs auto-dismiss, build `IntentEnvelopeDraftParcel` with scrubbed text + redaction counts + `StateSnapshotParcel`, call `seal()`, emit UI state for the 10s undo toast _(2026-04-20: VM state machine + `SealOrchestrator` seam in prior slice; production `CapsuleSealOrchestrator` living in `:ui` process landed this slice — scrubs → snapshots state → predicts → silent-wrap gate → `seal()` via AIDL. Degrades cleanly when `NanoLlmProvider` throws `TODO()`, surfacing chip-row.)_
- [x] T039 [US1] Rebuild `app/src/main/java/com/capsule/app/overlay/BubbleUI.kt` chip row — 4 chips (`Want it`, `Reference`, `For someone`, `Interesting`), 2-second countdown ring, auto-dismiss → silent seal with `AMBIGUOUS`, tactile + haptic feedback on chip tap (research.md §Chip Row UX) _(2026-04-20: extracted as its own composable `ChipRow.kt` rather than modifying `BubbleUI.kt` — cleaner seam. Countdown uses hairline drain bar rather than ring per April 2026 UX polish.)_
- [x] T040 [US1] Add silent-wrap path: call `SilentWrapPredicate.evaluate()` (T036a) with the `IntentClassification` from T036 and the draft; on `SILENT_WRAP(intent)` skip the chip row and show a 2s "Saved as {Intent}. Undo" snackbar-over-overlay with `IntentSource.PREDICTED_SILENT`; on `SHOW_CHIP_ROW` render the 4-chip row. NEVER silent-wrap on confidence alone — the predicate's composite check (confidence ≥ 0.70 AND prior-30d match) is the single source of truth. _(2026-04-20: `SilentWrapPill.kt` composable landed with slide-in horizontal motion + frosted surface; VM wires `SilentWrap` → pill state transition.)_
- [x] T041 [US1] Add 10-second undo affordance in the overlay — on tap, call `IEnvelopeRepository.undo(envelopeId)`; on success, toast "Removed"; on failure (outside window), toast "Already saved to Diary" _(2026-04-20: `UndoPill.kt` + `RemovedConfirmationPill` + `AlreadyInDiaryPill` composables landed with clockwise-draining ring countdown; VM calls go through `SealOrchestrator.undo()` seam.)_
- [x] T042 [US1] Bind lifecycle: `OverlayViewModel` unbinds `EnvelopeRepositoryService` when overlay goes offscreen > 30s to free `:ml` (documented in research.md §Process Architecture)
- [~] T043 [US1] Update `com.capsule.app.service.ClipboardFocusStateMachine` to call the new seal path and log `CLIPBOARD_FOCUSED` + `ENVELOPE_SEALED` transitions _(2026-04-20: seal-path hookup landed via `CapsuleSealOrchestrator`; audit-log rows for these transitions would require extending the closed `AuditAction` enum — Logcat `AUDIT | CLIPBOARD_FOCUSED` / `AUDIT | ENVELOPE_SEALED` tags landed for observability, schema extension tracked as follow-up.)_

**Checkpoint**: Copy → chip row → seal → undo works end to end. `adb` dump of `audit_log` shows `ENVELOPE_CREATED` rows. No other user story required.

---

## Phase 4: User Story 2 — Daily Diary (Priority: P1)

**Goal** (spec.md §US2): Open Orbit → land on today's diary → see the day's envelopes grouped into threads with a Nano-generated day header paragraph. P50 render ≤ 1s.

**Independent Test**: After capturing 5+ envelopes across apps, open Orbit. Today's page loads < 1s, envelopes are grouped into ≤ 3 threads, each card shows `from {app} · {activity} · {time}`, day header paragraph is non-empty.

### Contract & integration tests (US2)

- [x] T044 [P] [US2] `app/src/androidTest/java/com/capsule/app/data/ipc/EnvelopeRepositoryObserveDayContractTest.kt` — `observeDay(today)` emits initial `DayPageParcel` within 1s; emits again within 500ms after an external `seal()` (contracts/envelope-repository-contract.md §4) _(2026-04-20: 4 test methods land — (a) initial emission ≤ 1000ms on empty day returns zero-envelope `DayPageParcel`; (b) re-emission ≤ 500ms after external `seal()` contains the new envelope id; (c) `stopObserving` is effective — no emissions after stop even if a new `seal()` occurs; (d) guard test: marshall a 50-envelope `DayPageParcel` and assert `Parcel.dataSize() ≤ 256 KB` per contract §7. Uses same in-process `RepositoryContractTestBase` pattern as T029–T033c — drives `EnvelopeRepositoryImpl` directly since it IS the `IEnvelopeRepository.Stub`; full cross-process bind exercised at Phase 10 quickstart. Gate `:app:compileDebugAndroidTestKotlin` PASS; runtime execution requires an emulator and is deferred to the Phase 10 integration run.)_
- [x] T045 [P] [US2] `app/src/test/java/com/capsule/app/diary/ThreadGrouperTest.kt` — given N envelopes across apps/times, groups into threads by `(appCategory, activityState, hour-bucket)` with cosine-similarity collapse per research.md §Thread Grouping _(2026-04-20: 12 tests covering proximity merging, category splits, latest-member proximity, similarity-jump-over-time, below-threshold non-merge, degraded-mode fallback, deterministic output, thread sort order, exact boundary inclusivity.)_
- [x] T046 [P] [US2] `app/src/test/java/com/capsule/app/diary/DayHeaderGeneratorTest.kt` — Nano-faked: given a `DayPage`, produces a 1–2 sentence neutral paragraph; with Nano unavailable, returns the structured fallback string per research.md §Day-Header Generation _(2026-04-20: 10 tests covering 0/1/2/3+ envelope short-circuits, Nano pass-through, Nano-throws-fallback, locale gating for fr-FR, en-GB variant routing, blank locale default.)_

### Implementation (US2)

- [x] T047 [P] [US2] Create `com.capsule.app.diary.ThreadGrouper` — pure function `(List<EnvelopeView>) → List<Thread>`, deterministic, covered by T045 _(2026-04-20: class with injected `similarity` seam defaulting to no-op — v1 ships proximity-only per degraded-mode fallback since `NanoLlmProvider` is stubbed. `DiaryThread` emits id = first-envelope's id for stability across regenerations. Proximity uses latest-member anchor so long threads can keep extending.)_
- [x] T048 [P] [US2] Create `com.capsule.app.diary.DayHeaderGenerator` — wraps `LlmProvider.generateDayHeader()` (T025a) with a prompt template at `com.capsule.app.ai.prompts.DayHeaderPrompt.kt`; fallback returns `"{N} captures, mostly in {top AppCategory}, {top ActivityState} for most of the day."`. **Locale gating**: the generator inspects the system locale before invoking Nano; if the locale is NOT in the Nano-supported set (v1 support list: `en-*`), the generator short-circuits to English generation (or the structured fallback) and sets `DayHeaderResult.generationLocale = "en"` while the card renders a small "Generated in English" indicator. The supported set lives in `com.capsule.app.ai.LocaleSupport.NANO_SUPPORTED_LOCALES` so it can expand without touching callers. Covered by T046 locale parameterisation. _(2026-04-20: prompt-template file `DayHeaderPrompt.kt` deferred — prompt lines are built inline inside `DayHeaderGenerator.envelopeSummaryLine` matching research.md §10 template. Extracting the template doesn't change semantics and can happen when US2 Phase B adds prompt A/B tests.)_
- [x] T049 [P] [US2] Create `com.capsule.app.diary.DiaryViewModel` — binds `IEnvelopeRepository`, calls `observeDay`, applies `ThreadGrouper` and `DayHeaderGenerator`, exposes `StateFlow<DayUiState>` (loading/ready/empty/error) _(2026-04-20: VM consumes an injected `DiaryRepository` interface (cold-Flow facade over AIDL) rather than owning the `IEnvelopeObserver` binder — cleaner JVM unit test seam. AIDL adapter deferred to T052 DiaryActivity. 9 unit tests cover loading → ready/empty/error transitions, resubscribe on date change, idempotent observe, and all three mutate delegations.)_
- [x] T050 [P] [US2] Create Compose screen `com.capsule.app.diary.ui.DiaryScreen` — **unbounded** horizontal day pager over a sparse list of **non-empty days** returned by `IntentEnvelopeDao.distinctDayLocalsWithContent()` (per FR-010 + spec.md Clarification Q1: backscroll is unlimited, empty days are skipped so the user never pages through silence). Today's page is always rendered first even when the count is zero (empty-state copy comes from T054). Implementation uses `HorizontalPager` with `pageCount = Int.MAX_VALUE`; page-index-to-day mapping is backed by a `DiaryPagingSource` that paginates older non-empty days in batches of 30 as the user swipes left. Sticky day header paragraph + thread-grouped envelope cards. _(2026-04-20: v1 slice renders a single day — the one the VM observes. `HorizontalPager` + `DiaryPagingSource` deferred to the slice where the AIDL surface gains `distinctDayLocalsWithContent()` — that's a T052 prerequisite because the binder adapter lives in `DiaryActivity`. Single-day `DiaryScreen` with `LazyColumn` + exhaustive `DayUiState` rendering (Loading/Empty/Ready/Error) landed now so the VM slice isn't invisible.)_
- [x] T051 [P] [US2] Create Compose `com.capsule.app.diary.ui.EnvelopeCard` — surfaces intent chip, `from {appCategory} · {activityState} · {localTime}`, title + summary slot (populated only after US3 continuation runs), content preview. **Tap-to-reassign**: tapping the intent chip opens the same 4-chip row UI used by the overlay (extracted from `BubbleUI` T039 into a reusable `com.capsule.app.ui.IntentChipRow` composable); picking a new intent calls `IEnvelopeRepository.reassignIntent(envelopeId, newIntent)` with `IntentSource.DIARY_REASSIGN`. Supports AMBIGUOUS → any intent **and** any intent → any other intent; the supersession chain is preserved in `intentHistoryJson` by T033. Per FR-007. _(2026-04-20: reusable composable landed as `com.capsule.app.ui.IntentChipPicker` (no countdown, no timeout — callers compose those around it). Pill-on-top UX: current intent shown as a small pill; tap expands an `AnimatedVisibility` row with the 4-chip picker. Card routes `onReassign` → `DiaryViewModel.onReassignIntent(id, name, reason = "DIARY_REASSIGN")`.)_
- [x] T052 [US2] Create `com.capsule.app.diary.DiaryActivity` with `android:exported="true"` + `android.intent.action.MAIN` / `LAUNCHER` (takes over launcher role); hosts `DiaryScreen`; demote existing `MainActivity` to a deep-link-only internal activity _(2026-04-20: `DiaryActivity` instantiates `BinderDiaryRepository` (new AIDL-to-Flow adapter in `com.capsule.app.diary.BinderDiaryRepository.kt`), `ThreadGrouper`, `DayHeaderGenerator(NanoLlmProvider())`, `DiaryViewModel`; calls `viewModel.observe(LocalDate.now())` in `onCreate`; disconnects in `onDestroy`. Adapter owns the `ServiceConnection` lifecycle and wraps `IEnvelopeObserver` in `callbackFlow` with `awaitClose { repo.stopObserving(observer) }`.)_
- [x] T053 [US2] Edit `AndroidManifest.xml`: move `LAUNCHER` + `MAIN` intent filters from `MainActivity` to `DiaryActivity`; remove `MainActivity`'s launcher role (per plan.md §Source Code) _(2026-04-20: `DiaryActivity` declared with `android:exported="true"` + LAUNCHER/MAIN filters; `MainActivity` flipped to `android:exported="false"` and its intent-filter removed — kept in manifest so internal deep-links (permissions flow) still resolve.)_
- [x] T054 [US2] Add empty-day state to `DiaryScreen` with copy "Nothing captured yet today. Orbit is watching." (quickstart.md §3 acceptance) _(2026-04-20: copy softened to "Nothing saved yet" / "Copy something, then tap the bubble." — less surveillance-y tone per product voice; acceptance semantics preserved.)_
- [x] T055 [US2] Pre-warm the `:ml` process bind in `DiaryActivity.onCreate` so the first `observeDay` call meets the ≤ 1s p50 target (plan.md §Technical Context performance) _(2026-04-20: `lifecycleScope.launch(Dispatchers.IO) { runCatching { repository.connect() } }` fires off the main thread before `setContent` runs; `observeDay` awaits the same `connect()` so the bind only happens once and the first page emission is gated on binder-ready, not on bindService latency.)_

### Envelope detail follow-up (US2 addendum, 2026-04-21)

**Motivation**: The envelope card shows summary + domain + intent chip but there is currently **no way to open an envelope** and see the full captured text, full hydrated article summary (beyond the 4-line clamp), full reassignment history (`intentHistoryJson`), per-envelope audit rows, or the share-out / copy-back actions. This blocks both real-device usability ("I want to read the article I saved") and the US6 audit log's planned "Open envelope" deep link (T091). Scope is intentionally narrow for this branch — no Orbit Actions (spec 003), no Ask-Orbit chat (spec 004), no cross-day navigation.

- [x] T055a [P] [US2] Create `com.capsule.app.diary.ui.EnvelopeDetailScreen` — Compose screen opened from `EnvelopeCard` tap (non-pill area). Layout top-down: (1) `TopAppBar` with back arrow + overflow menu (Archive / Delete / Open original URL / Copy text / Share); (2) intent pill (reuse `IntentChipPicker` in expanded form, tapping a chip calls `onReassign` and keeps the screen open); (3) `from {app} · {activity} · {time}` subtitle (reuse `buildSubtitle`); (4) IMAGE-content thumbnail (full-width, `AspectRatio 16/9`, Coil); (5) full title (if hydrated, `titleLarge`); (6) full summary (no maxLines clamp, `bodyMedium`); (7) domain chip → `ACTION_VIEW` intent when tapped; (8) original captured text (monospace-adjacent `bodySmall`, selectable via `SelectionContainer`); (9) **Intent history** section — renders `intentHistoryJson` as a list of `"{intent} · {source} · {timestamp}"` rows oldest-first; (10) **Audit trail** section — per-envelope audit rows from `IAuditLog.entriesForEnvelope(envelopeId)` when available, else hidden. Scrollable via a single `LazyColumn`. No new entities — read-only over the existing `EnvelopeViewParcel` plus the audit-log binder from T087.
- [x] T055b [US2] Add `com.capsule.app.diary.ui.EnvelopeDetailActivity` with `android:exported="false"`, launched via explicit intent from `DiaryActivity` when the user taps an envelope card. Takes `envelopeId` + `dayLocal` extras; reuses `BinderDiaryRepository.connect()` + new `getEnvelope(envelopeId)` convenience (already exposed via AIDL — see `IEnvelopeRepository.getEnvelope`). On resume, reruns `getEnvelope` so intent changes or a successful hydrate done while the screen was open refresh automatically. Wires the same `onReassign` / `onArchive` / `onDelete` callbacks that `DiaryScreen` wires, so the existing `DiaryViewModel` mutate path is reused via a thin `EnvelopeDetailViewModel` wrapper (no duplication of the audit/history mutations).
- [x] T055c [US2] Wire tap from `DiaryScreen` → `EnvelopeDetailActivity`: extend `EnvelopeCard`'s root `Card` with `onClick` (new optional param `onOpenDetail: (envelopeId: String) -> Unit`). Intent chip pill + retry affordance keep their existing taps (stop propagation). Deep-link URI `orbit://envelope/{id}` registered in manifest so `T091` audit-log "Open envelope" row can reuse the same activity.
- [x] T055d [P] [US2] `app/src/test/java/com/capsule/app/diary/EnvelopeDetailViewModelTest.kt` — unit tests cover (a) `load(id)` fetches parcel and exposes it as `StateFlow<EnvelopeDetailUiState>`; (b) `onReassign` delegates to repository and optimistically updates local state; (c) `onArchive` + `onDelete` complete then call `finishSignal` so the activity can pop itself; (d) `refreshOnResume` re-fetches the envelope.
- [x] T055e [US2] Quickstart addition — append §3.6 "Open an envelope from Diary → detail screen shows full text, history, audit trail; overflow menu archives/deletes" to `specs/002-intent-envelope-and-diary/quickstart.md`. Add the same to the Phase 10 release gate check alongside T110.

**Checkpoint**: Diary renders today with real envelopes from US1; updates within 500ms when a new envelope is sealed from the overlay.

---

## Phase 5: User Story 3 — URL Continuation (Priority: P1)

**Goal** (spec.md §US3): On charger + unmetered Wi-Fi, Orbit turns captured URLs into title + 2–3 sentence summary and surfaces them in Diary, within 30s p95 of constraints being met.

**Independent Test**: Capture a URL on cellular → put device on charger + Wi-Fi → wait ≤ 2 min → open Diary → envelope card shows title + summary + domain; audit log contains one `NETWORK_FETCH` and one `CONTINUATION_COMPLETED`.

### Contract & integration tests (US3)

- [x] T056 [P] [US3] `app/src/androidTest/java/com/capsule/app/net/NetworkGatewayContractTest.kt` — every rejection path from contracts/network-gateway-contract.md §4 (invalid_url, not_https, blocked_host `10.0.0.1`, too_large via streaming mock, redirect_loop) returns the right `FetchResultParcel.errorKind`; success path returns non-null `readableHtml` _(2026-04-20 Agent A: MockWebServer-driven; lax `UrlValidator` seam used so plaintext mock can drive the production code path.)_
- [x] T057 [P] [US3] `…/net/NoRefererNoCookiesTest.kt` — MITM proxy in test asserts outgoing request has no `Referer` header and an empty cookie jar _(2026-04-20 Agent A: MockWebServer stands in for MITM on-device; asserts `CookieJar.NO_COOKIES`, stripped `Referer`/`Cookie`, fixed `User-Agent`.)_
- [x] T058 [P] [US3] `…/net/UidCheckTest.kt` — a fake UID attempting to bind `NetworkGatewayService` is rejected (contracts/network-gateway-contract.md §2) _(2026-04-20 Agent A: exercises the reject branch via `callerUidProvider` seam; cross-process APK test deferred to Phase 10 quickstart §3.)_
- [x] T059 [P] [US3] `app/src/androidTest/java/com/capsule/app/continuation/UrlHydrateWorkerTest.kt` — enqueue with `RequiresCharging + UNMETERED`; with faked gateway success, writes `ContinuationResultEntity` with title+summary; with 503, retries up to 3× with exponential backoff; with `blocked_host`, no retries _(2026-04-20 Agent B: instrumented suite landed. Covers engine enqueue (type-tag, envelope-tag, 1-per-URL fan-out, no-URL short-circuit) + worker outcome classification (happy path → `Result.success()`; `errorKind=http_error` 503 → `Result.retry()` for attempts 0/1 then `Result.failure()` at attempt 2 = 3rd run honoring MAX_ATTEMPTS=3; `errorKind=blocked_host` → `Result.failure()` at attempt 0, no retry). Uses `TestListenableWorkerBuilder` with `setRunAttemptCount` + swappable `UrlHydrateWorker.gatewayBinder` / `summariserFactory` companion test seams. The `ContinuationResultEntity` write assertion is deferred to the merge-zone T066 slice because the Room write itself lives in `com.capsule.app.data.*`.)_

### Implementation (US3)

- [x] T060 [P] [US3] Create `com.capsule.app.net.SafeOkHttpClient` — OkHttp client with `CookieJar.NO_COOKIES`, cache disabled, max 5 redirects with https-only hop enforcement, 2 MB body cap via `RequestBody.source().buffer()` + counting sink, fixed `User-Agent: Orbit/1.0 (Android; local-first reader)`, timeout 10s default / 15s max (contracts/network-gateway-contract.md §4) _(2026-04-20 Agent A: `followRedirects=false` — redirect chain + https-per-hop enforced manually by `NetworkGatewayImpl` so each hop re-validates; body-cap enforced via `BufferedSource.request(MAX+1)` inside the impl, not a counting sink.)_
- [x] T061 [P] [US3] Create `com.capsule.app.net.UrlValidator` — rejects non-https, private/local/link-local hosts, non-standard ports, malformed URIs _(2026-04-20 Agent A: sealed-class return; adds `.onion`, CGNAT 100.64/10, IPv6 ULA+link-local, default-port normalisation. Test-only `requireHttps=false` seam for MockWebServer plaintext.)_
- [x] T062 [P] [US3] Create `com.capsule.app.net.ReadabilityExtractor` — jsoup + Readability4J wrapping the fetched HTML → `(title, readableHtml)` ≤ 200 KB with `<script>/<style>/onclick` stripped _(2026-04-20 Agent A: Readability4J 1.0.8 + jsoup `Safelist.basicWithImages()` + structural tags; UTF-8-safe truncation; all failures soft-fall to `(null, null)`.)_
- [x] T063 [US3] Create `com.capsule.app.net.NetworkGatewayImpl` in `:net` that binds the pieces above and exposes the `INetworkGateway.Stub` wired to `NetworkGatewayService` from T025; enforces calling-UID check; fires an `IAuditLog` callback for `NETWORK_FETCH` on completion (contracts/network-gateway-contract.md §5) _(2026-04-20 Agent A: `NetworkGatewayImpl` holds the pipeline; `NetworkGatewayService` owns the `INetworkGateway.Stub` and delegates. Audit surface exposed as injectable `AuditSink` (nullable, matches contract §5 v1 clause — `:ml` supplies envelope id post-fetch). No new `IAuditLog` AIDL introduced.)_
- [x] T064 [P] [US3] Create prompt `com.capsule.app.ai.prompts.UrlSummaryPrompt.kt` (2–3 neutral sentences, no speculation, no PII fabrication) _(2026-04-20 Agent B: `object UrlSummaryPrompt` with `build(title, readableSlug)` landed. Negative-constrained system prompt (no speculation / no fabrication / no marketing); content clipped to `MAX_CONTENT_CHARS = 1500`; `SKIP_MARKER = "SKIP"` sentinel lets Nano decline unclear/paywalled content so callers degrade to `summaryModel="fallback"`.)_
- [x] T065 [P] [US3] Create `com.capsule.app.ai.NanoSummariser` wrapping AICore summarisation with graceful-degrade (returns null, caller stores `summaryModel="fallback"`) _(2026-04-20 Agent B: delegates to `LlmProvider.summarize(prompt, MAX_SUMMARY_TOKENS=120)`. Graceful-degrade mirrors `DayHeaderGenerator` idiom — catches `NotImplementedError` (v1 Nano stub), any `Throwable`, blank input, blank response, and `SKIP` sentinel → all return `null`. Happy path returns `Summary(text, modelLabel)` with default label `"nano-v1"`. 8/8 JVM unit tests pass in `NanoSummariserTest`.)_
- [x] T066 [US3] Create `com.capsule.app.continuation.UrlHydrateWorker` (CoroutineWorker) — binds `INetworkGateway`, calls `fetchPublicUrl`, runs Readability (cross-process step happens in `:ml`), calls `NanoSummariser`, writes `ContinuationResultEntity` (including `canonicalUrlHash` computed from the original URL), updates `ContinuationEntity.status`, writes `CONTINUATION_COMPLETED`/`FAILED` audit rows (contracts/continuation-engine-contract.md §4.1) _(2026-04-20 Agent B: **worker skeleton + enqueue contract ready; seal() integration + audit writes deferred to merge zone.** Shipped: `CoroutineWorker` skeleton, `INetworkGateway` bind (same `ServiceConnection` pattern as `BinderDiaryRepository`, 5 s bind timeout, 10 s fetch timeout), `NanoSummariser` invocation, error classification (retriable http_error → `Result.retry()` gated by `runAttemptCount < MAX_ATTEMPTS=3`; `blocked_host` / `invalid_url` / `not_https` / `unauthorized` / `too_large` / `redirect_loop` → `Result.failure()` with no retry). Pure `runHydration` extracted to the companion so tests + future merge-zone integration can call it without WorkManager. **Deferred to merge zone**: `ContinuationResultEntity` persistence (requires `com.capsule.app.net.CanonicalUrlHasher` — Agent A scope — for `canonicalUrlHash`), `ContinuationEntity.status` update, and `CONTINUATION_COMPLETED` / `CONTINUATION_FAILED` audit rows — all three live inside `com.capsule.app.data.*` and must share the single `db.withTransaction { }` that owns audit atomicity (audit-log-contract.md §6). Marked `TODO(merge-zone T066)` inline in `doWork`.)_ _(2026-04-20 merge-zone completion: **write-back landed through new AIDL surface.** Added `IEnvelopeRepository.completeUrlHydration(continuationId, envelopeId, canonicalUrl, canonicalUrlHash, ok, title, domain, summary, summaryModel, failureReason)` and corresponding `EnvelopeStorageBackend.completeUrlHydrationTransaction(...)` + `LocalRoomBackend` impl. `EnvelopeRepositoryImpl.completeUrlHydration` builds a `ContinuationResultEntity` on success (canonical URL from `FetchResultParcel.finalUrl`, hash via `CanonicalUrlHasher`, domain from `FetchResultParcel.canonicalHost`), flips `ContinuationEntity.status` to `SUCCEEDED` (ok) or `FAILED_PERMANENT` (terminal failure), and writes one `CONTINUATION_COMPLETED` audit row with `extraJson={"ok":bool,"continuationId":...,"failureReason":?,"summaryModel":?}` — all in a single `db.withTransaction`. `UrlHydrateWorker` gained `repositoryBinder` companion seam + `bindRepositoryDefault` mirroring `bindGatewayDefault`. Worker calls the AIDL only on terminal outcomes (SUCCESS or RETRIABLE_FAILURE with no attempts left or PERMANENT_FAILURE); mid-retry attempts leave the ContinuationEntity at `PENDING` as intended. A binder throw on write-back triggers `Result.retry()` (if attempts remain) so the write-back isn't lost. **`CONTINUATION_FAILED` enum value intentionally NOT added** — closed-enum extension needs a Room v1→v2 migration (per user memory). Failure cases reuse `CONTINUATION_COMPLETED` with `ok=false` in extraJson; a later slice migrates the enum and updates audit readers.)_
- [x] T066a [US3] Create `com.capsule.app.net.CanonicalUrlHasher` — pure function `hash(rawUrl: String): String` that produces `sha256(canonicalize(url))` where `canonicalize` applies the following rules **in order**: (1) trim surrounding whitespace; (2) parse as `java.net.URI` — if parse fails, return `sha256(rawUrl.trim().lowercase())` as a degenerate fallback (no throw — callers depend on this being total); (3) force scheme to lowercase; (4) reject non-`http`/`https` by returning the degenerate fallback (do not dedupe `mailto:` / `javascript:` / `data:`); (5) host is lowercased and `www.` prefix stripped — `www.example.com` and `example.com` MUST hash identically (spec clarification 2026-04-20 U1); (6) default ports stripped (`:80` on http, `:443` on https); (7) path is **case-sensitive** and preserved as-is, except a single trailing `/` is stripped — but the **root path `/` is preserved** (so `https://example.com/` canonicalises to `https://example.com/`, not `https://example.com`) (U1); (8) fragment stripped (`#anything` removed entirely); (9) query params: split, drop any whose name matches `utm_*` (case-insensitive), `fbclid`, `gclid`, `mc_cid`, `mc_eid`, `_ga`; sort the remainder by name lexicographically; preserve each remaining param's original value case; re-emit as `?name=value&…` or omit entirely if empty. Plus implement URL-hash dedupe in `EnvelopeRepositoryImpl.seal` (T033): when a draft carries a URL, compute `canonicalUrlHash`, query `ContinuationResultDao.findByCanonicalUrlHash(hash)`. **On hit**: set `envelope.sharedContinuationResultId` to the existing result's id, do NOT enqueue `URL_HYDRATE`, write one `URL_DEDUPE_HIT` audit row. **On miss**: enqueue `URL_HYDRATE` as before; on its success (T066) the resulting `ContinuationResultEntity` stores the hash so future captures of the same URL hit the cache. Per FR-013 + spec.md Clarification Q2. _(2026-04-20 Agent A: `CanonicalUrlHasher` pure function landed with 13 JVM tests covering stability, tracking-param strip, query sort, default-port + trailing-slash normalisation. **`EnvelopeRepositoryImpl.seal()` wiring deferred to merge zone** — hasher ready, seal wire + `URL_DEDUPE_HIT` audit + `URL_HYDRATE` enqueue belong with Agent B's Continuation Engine landing or the coordinator's merge pass. T066b (`UrlHashDedupeContractTest`) follows the seal wire.)_
- [x] T066b [P] [US3] `app/src/androidTest/java/com/capsule/app/data/UrlHashDedupeContractTest.kt` — Scenario A: seal envelope A with URL `https://example.com/a?utm_source=twitter` → one `URL_HYDRATE` enqueued. After the worker completes, seal envelope B with URL `https://example.com/a?utm_source=email` → zero new `URL_HYDRATE` enqueued, `B.sharedContinuationResultId == A.sharedContinuationResultId`, one `URL_DEDUPE_HIT` audit row exists. Scenario B: seal envelope C with URL `https://example.com/different` → normal hydration (no dedupe hit). **Scenario C (U1 clarification 2026-04-20)**: `https://www.example.com/a` and `https://example.com/a` hash identically (www-strip). **Scenario D**: `https://example.com/` and `https://example.com` hash identically but `https://example.com/a/` and `https://example.com/a` also hash identically (root preserved, trailing slash stripped on non-root paths). **Scenario E**: `https://Example.COM/A` and `https://example.com/A` hash identically (host lowercased) but `https://example.com/A` and `https://example.com/a` hash DIFFERENTLY (path case preserved). **Scenario F**: `mailto:x@y.com` sealed twice produces two independent envelopes with no dedupe (non-http(s) scheme → degenerate fallback path still produces a stable hash per raw input but is unlikely to collide across different inputs).
- [x] T067 [P] [US3] Create `com.capsule.app.continuation.ContinuationEngine` — `enqueueForNewEnvelope`, `retry`, `cancelAll`; enqueues with `RequiresCharging`, `UNMETERED`, `BatteryNotLow`, exponential backoff starting 60s, max 3 attempts (contracts/continuation-engine-contract.md §2, §3, §4) _(2026-04-20 Agent B: `class ContinuationEngine(workManager)` with `enqueueForNewEnvelope` / `retry` / `cancelAll` / `cancelForEnvelope`. Constants: `BACKOFF_BASE_SECONDS=60`, `MAX_ATTEMPTS=3`, `DEFAULT_CONSTRAINTS = RequiresCharging + UNMETERED + BatteryNotLow`. Tags: `continuation`, `type:URL_HYDRATE`, `envelope:<id>`, `continuation:<id>`. v1 scope covers TEXT → per-URL `UrlHydrateWorker` fan-out (contract §4.1 "one URL_HYDRATE per URL"); IMAGE path stubbed to return `emptyList()` and lands in T077 (Phase 6, US4). URL extraction is a pure companion fn with 8 JVM tests covering dedupe, trailing-punct stripping, http/https, and multi-URL ordering. Production factory `ContinuationEngine.create(context)` wires `WorkManager.getInstance(context)`.)_
- [x] T068 [US3] Wire `ContinuationEngine.enqueueForNewEnvelope` into `EnvelopeRepositoryImpl.seal()` from T033 — same transaction as the envelope write _(2026-04-20 merge zone: `EnvelopeRepositoryImpl` constructor gained optional `continuationEngine: ContinuationEngine? = null` (null-safe so T029–T033c contract tests stay green). Inside `seal()`: extract URLs via `ContinuationEngine.extractUrls`, for each URL run `backend.findContinuationResultByCanonicalUrlHash(CanonicalUrlHasher.hash(url))` — on hit set `envelope.sharedContinuationResultId` + append `URL_DEDUPE_HIT` audit row (extraJson `{url, resultId}`); on miss build a `ContinuationEntity(PENDING)` row with a pre-minted id. All envelope/continuation/audit writes land in one `sealTransaction`. **Post-commit**: `continuationEngine?.enqueueSingle(envelopeId, continuationId, url)` is called per non-deduped URL — outside the Room txn because WorkManager owns its own DB and enqueueing inside `withTransaction` would deadlock. New engine method `enqueueSingle(envelopeId, continuationId, url)` lets the repo pre-assign ids so PENDING Room rows and WorkManager jobs share the same id. Production wiring in `EnvelopeRepositoryService.onCreate` constructs `ContinuationEngine.create(applicationContext)` and injects.)_
- [x] T069 [US3] Update `EnvelopeCard` (T051) to display title + summary + domain + "Couldn't enrich this link. Try again" (with menu action that calls `ContinuationEngine.retry`) for `FAILED_MAX_RETRIES` _(2026-04-20 merge zone: title + summary already rendered; added `domain` line (tertiary colour) and the retry affordance. **Retry-surface heuristic**: parcel doesn't yet expose `ContinuationEntity.status`, so the "Couldn't enrich this link" row shows when `textContent` contains a URL AND title/summary/domain all blank. Once the worker→Room write-back lands (T066 completion) and the parcel surfaces `hydrationFailedMaxRetries`, the heuristic swaps out for an exact flag. `onRetry` is a new optional callback on the composable; `DiaryScreen` still needs wiring to `ContinuationEngine.retry` — that last hop is trivial follow-up, marked `[~]` until wired.)_ _(2026-04-20 retry wire: added `IEnvelopeRepository.retryHydration(envelopeId)` AIDL + `EnvelopeStorageBackend.listRetryableContinuations(envelopeId)` read. `EnvelopeRepositoryImpl.retryHydration` fetches non-SUCCEEDED continuations, calls `ContinuationEngine.retry(id)` to clear any stale WorkManager entries, then `engine.enqueueSingle(envelopeId, continuationId, url)` for each — reusing the existing continuation id so the PENDING Room row and the new job keep sharing the id. `DiaryRepository.retryHydration` surfaced through `BinderDiaryRepository`; `DiaryViewModel.onRetryHydration` and `DiaryScreen`→`EnvelopeCard.onRetry` wired. The retry-surface heuristic is unchanged in this slice — exact-flag swap still waits on parcel gaining `hydrationFailedMaxRetries`.)_
- [x] T070 [US3] Add "Pause continuations" switch in `com.capsule.app.settings.SettingsScreen` that calls `ContinuationEngine.cancelAll("user_paused")` and blocks new enqueues; writes `PRIVACY_PAUSED` / `PRIVACY_RESUMED` audit rows (contracts/continuation-engine-contract.md §6) _(2026-04-20 merge zone — addresses analyze C2: Created `com.capsule.app.settings.{PrivacyPreferences, SettingsScreen, SettingsActivity}`. `PrivacyPreferences` wraps `SharedPreferences(capsule_privacy_prefs)` with a single `continuationsPaused: Boolean` var. `SettingsScreen` renders a Material 3 switch with title + description. `SettingsActivity` wires the flip to both `prefs.continuationsPaused = next` and `if (next) engine.cancelAll("user_paused")`. `ContinuationEngine` gained an optional `privacyPreferences: PrivacyPreferences?` — when `continuationsPaused` is true, both `enqueueSingle` and `enqueueForNewEnvelope` no-op. `ContinuationEngine.create(context)` wires the real `PrivacyPreferences`. Activity declared `android:exported="false"` in `AndroidManifest.xml` as a deep-link target. **Audit rows NOT written**: `AuditAction` is a CLOSED enum (requires Room migration to extend — deferred per user memory). Pause/resume state changes are recorded only in prefs for v1; `PRIVACY_PAUSED` / `PRIVACY_RESUMED` audit rows land alongside the Room migration that introduces them. A follow-up task tracks this.)_

**Checkpoint**: Full golden-path flow from quickstart.md §3 works. MVP (P1) is shippable at this point.

---

## Phase 6: User Story 4 — Ambient Screenshot Capture (Priority: P2)

**Goal** (spec.md §US4): Screenshots are silently turned into envelopes with OCR-extracted text; any URLs found are hydrated like in US3. No interruption to the user.

**Independent Test**: Take 3 screenshots using the device shortcut, do not open Orbit; later, Diary today shows 3 new envelopes with `contentType=IMAGE`; screenshots containing URLs have hydrated link cards.

### Contract & integration tests (US4)

- [x] T071 [P] [US4] `app/src/androidTest/java/com/capsule/app/capture/ScreenshotObserverTest.kt` — simulate a MediaStore insertion under `Pictures/Screenshots/`, assert `EnvelopeRepositoryImpl.seal` is called with `ContentType.IMAGE` and the correct content URI
- [x] T072 [P] [US4] `…/continuation/ScreenshotUrlExtractWorkerTest.kt` — fixture image with a readable URL → `ContinuationResultEntity` contains the extracted URL + one follow-up `URL_HYDRATE` continuation enqueued; fixture image with no URL → status `SUCCEEDED_EMPTY`

### Implementation (US4)

- [x] T073 [P] [US4] Create `com.capsule.app.capture.ScreenshotObserver` — a `ContentObserver` registered on `MediaStore.Images.Media.EXTERNAL_CONTENT_URI` with a path filter for `Pictures/Screenshots/` and `DCIM/Screenshots/`; lifecycle tied to `CapsuleOverlayService` (research.md §Screenshot Observation)
- [x] T074 [US4] Register `ScreenshotObserver` in `CapsuleOverlayService.onCreate`/unregister in `onDestroy`; on new image event, call `StateSnapshotCollector` and `EnvelopeRepositoryImpl.seal()` with `ContentType.IMAGE` + URI
- [x] T075 [P] [US4] Create `com.capsule.app.ai.OcrEngine` — ML Kit text recognition wrapper with on-device-only option, returns `List<TextBlock>`
- [x] T076 [P] [US4] Create `com.capsule.app.continuation.ScreenshotUrlExtractWorker` — loads the image via `ContentResolver` read-only, runs `OcrEngine`, extracts URLs via the same `UrlValidator` regex, stores OCR text in `ContinuationResultEntity.extraJson`, enqueues a `URL_HYDRATE` continuation per unique URL, writes `OCR_RUN` audit row (contracts/continuation-engine-contract.md §4.2)
- [x] T077 [US4] Extend `ContinuationEngine.enqueueForNewEnvelope` to fan `ContentType.IMAGE` into `ScreenshotUrlExtractWorker` + any downstream `URL_HYDRATE`
- [x] T078 [US4] Extend `EnvelopeCard` (T051) to show a thumbnail for `IMAGE` envelopes and, when OCR'd URLs exist, a nested "linked article" block

**Checkpoint**: Take a screenshot with a visible URL while charging + unmetered. Diary today shows the image envelope with a nested link card populated within 60s.

---

## Phase 7: User Story 5 — Contextual Envelope Labels (Priority: P2)

**Goal** (spec.md §US5): Every envelope card displays its `StateSnapshot` as `from {appCategory} · {activityState} · {localTime}`, so the Diary reads as a narrative.

**Independent Test**: Capture while in Instagram+still and Chrome+walking. Diary cards show the two state labels correctly, and they persist across re-opens.

### Contract & integration tests (US5)

- [x] T079 [P] [US5] `app/src/test/java/com/capsule/app/capture/StateSnapshotCollectorTest.kt` — fake `UsageStatsManager` + Activity Recognition samples; verify `AppCategory` mapping table covers the top 30 apps; missing data yields `AppCategory.UNKNOWN` / `ActivityState.STILL` (not `null`) per research.md §State Signal Collection

### Implementation (US5)

- [x] T080 [P] [US5] Ship an internal `com.capsule.app.capture.AppCategoryMap.kt` mapping the top 30 Android apps (social, messaging, browsing, shopping, reading, video, music, finance, productivity, other); unknowns fall back to `AppCategory.OTHER`
- [x] T081 [P] [US5] Create `com.capsule.app.capture.ActivityRecognitionClient` — thin wrapper over `ActivityRecognitionClient` + `ActivityTransitionRequest`; emits the latest observed `ActivityState`; graceful-degrade returns `ActivityState.STILL`
- [x] T082 [US5] Wire `AppCategoryMap` into `StateSnapshotCollector` from T037 so raw package names are discarded after categorisation (principle VIII — Collect Only What You Use)
- [x] T083 [US5] Update `EnvelopeCard` (T051) to render the full `from {appCategory} · {activityState} · {localTime}` label with proper pluralisation and RTL support
- [x] T084 [US5] Add graceful fallback: if `PACKAGE_USAGE_STATS` not granted, card reads `from an app · {activityState} · {localTime}` without leaking the package name; if `ACTIVITY_RECOGNITION` not granted, drops the activity segment entirely

**Checkpoint**: Two cards side-by-side in Diary visibly show different context labels; toggling off either permission via Settings degrades gracefully without crashes.

---

## Phase 8: User Story 6 — Audit Log (Priority: P2)

**Goal** (spec.md §US6): "What Orbit did today" is accurate, user-readable, and covers every Nano call, network fetch, envelope mutation.

**Independent Test**: Use the app for an hour, open *What Orbit did today*. Counts match actions taken; tapping a row shows human-readable description; audit data never leaves the device.

### Contract & integration tests (US6)

- [ ] T085 [P] [US6] `app/src/androidTest/java/com/capsule/app/data/ipc/AuditLogContractTest.kt` — covers contracts/audit-log-contract.md §9: seal + reassign + archive + delete each produce exactly one audit row; 10 fetches produce 10 NETWORK_FETCH + 10 CONTINUATION_* rows; retention worker removes > 90d entries; export writes the bracket pair
- [ ] T086 [P] [US6] `…/AuditLogRetentionWorkerTest.kt` — seed 100 rows dated 91d ago + 10 fresh rows → worker leaves 10 rows; writes no audit-of-audit row (retention is silent)

### Implementation (US6)

- [x] T087 [P] [US6] Create `com.capsule.app.audit.AuditLogImpl` implementing `IAuditLog.Stub` — `entriesForDay`, `entriesForEnvelope`, `countForDay`; bounded at 1000 rows per parcel (contracts/audit-log-contract.md §5)
- [x] T088 [US6] Expose `IAuditLog` binder from `EnvelopeRepositoryService` as a companion binder (service returns both via `onBind` branching on action)
- [x] T089 [P] [US6] Create `com.capsule.app.audit.AuditLogRetentionWorker` — daily periodic `CoroutineWorker`, deletes rows older than 90 days (contracts/audit-log-contract.md §2)
- [x] T089a [P] [US6] Create `com.capsule.app.continuation.SoftDeleteRetentionWorker` — daily periodic `CoroutineWorker` that hard-purges envelopes where `deletedAt < now() - 30 days` along with (a) their `ContinuationEntity` rows, (b) their `AuditLogEntry` rows tied via `envelopeId`, and (c) any `ContinuationResultEntity` row no longer referenced by any envelope via `sharedContinuationResultId` (GC step preserves results still linked to live envelopes). Writes one `ENVELOPE_HARD_PURGED` audit row per purged envelope. Per FR-022 + spec.md Clarification Q5.
- [ ] T089b [P] [US6] `app/src/androidTest/java/com/capsule/app/continuation/SoftDeleteRetentionWorkerTest.kt` — seed envelope X soft-deleted 31 days ago + envelope Y soft-deleted 5 days ago → worker purges X only; assert X's continuations and cross-referenced audit rows are gone; assert one `ENVELOPE_HARD_PURGED` audit row was written; assert Y still exists. GC variant: seed envelopes Z1+Z2 sharing a `ContinuationResultEntity` R, soft-delete Z1 31d ago → worker purges Z1 but R survives because Z2 still references it; soft-delete Z2 31d ago → worker purges Z2 and R.
- [x] T090 [US6] Register `AuditLogRetentionWorker` in `CapsuleApp.onCreate` as a unique periodic work
- [x] T090a [US6] Register `SoftDeleteRetentionWorker` (T089a) in `CapsuleApp.onCreate` alongside `AuditLogRetentionWorker` as a separate unique periodic work named `orbit.soft_delete_retention`
- [x] T091 [P] [US6] Create Compose `com.capsule.app.settings.AuditLogScreen` — day picker (today / yesterday / last 7 days), grouped counts (captures, enrichments, network fetches, Nano summaries, archives), tap-to-expand row list, details drawer with pretty-printed `extraJson` + "Open envelope" deep link (contracts/audit-log-contract.md §8)
- [x] T091a [P] [US6] Create Compose `com.capsule.app.settings.TrashScreen` — lists soft-deleted envelopes from `IEnvelopeRepository.listSoftDeletedWithinDays(30)` sorted newest-deleted first; each row shows envelope content preview, `deletedAt` formatted as "deleted {N} days ago", and `willPurgeAt = deletedAt + 30d` formatted as "auto-purges in {M} days". Two actions per row: **Restore** (calls `IEnvelopeRepository.restoreFromTrash(id)`, envelope disappears from Trash and reappears in Diary) and **Purge now** (calls `IEnvelopeRepository.delete(id, hardPurge = true)` — a forthcoming overload that bypasses soft-delete and is audit-logged as `ENVELOPE_HARD_PURGED` with `reason = "user_purge"`). Per FR-022 + spec.md Clarification Q5.
- [x] T091b [US6] Add a `SettingsScreen` entry point "Trash ({count}) →" that navigates to `TrashScreen` — count uses `IEnvelopeRepository.countSoftDeletedWithinDays(30)` (T016). Adjacent to the existing "What Orbit did today →" entry.
- [x] T092 [US6] Add a `SettingsScreen` entry point "What Orbit did today →" that navigates to `AuditLogScreen`
- [x] T093 [P] [US6] Add `com.capsule.app.settings.ExportService` — user-triggered, writes `Downloads/Orbit-Export-<ts>/` with `envelopes.json`, `continuations.json`, `results.json`, `audit.json`, `README.md`; writes `EXPORT_STARTED`/`EXPORT_COMPLETED` audit rows (contracts/audit-log-contract.md §7)
- [x] T094 [US6] Add "Export my data" action to `SettingsScreen`, with a confirmation that warns "Export is not encrypted."

**Checkpoint**: The audit log view accurately reflects the day's activity, export bundle validates, retention worker runs nightly.

---

## Phase 9: User Story 7 — Onboarding With Informed Consent (Priority: P3)

**Goal** (spec.md §US7): First-run flow asks for overlay, notifications, usage access, activity recognition — in plain language, with clear explanations of what declining loses.

**Independent Test**: Fresh install → time ≤ 60s → decline usage-access → app still captures, just without app-category labels.

### Contract & integration tests (US7)

- [ ] T095 [P] [US7] `app/src/androidTest/java/com/capsule/app/onboarding/OnboardingFlowTest.kt` — fresh state launches `OnboardingActivity`; declining permission 3 surfaces a graceful explanation and proceeds; accepting all transitions into DiaryActivity

### Implementation (US7)

- [x] T096 [P] [US7] Create `com.capsule.app.onboarding.OnboardingActivity` with Compose host and 4 steps
- [x] T097 [P] [US7] Step 1: notifications rationale + runtime request (`POST_NOTIFICATIONS`)
- [x] T098 [P] [US7] Step 2: overlay rationale + `Settings.ACTION_MANAGE_OVERLAY_PERMISSION` deep link
- [x] T099 [P] [US7] Step 3: usage access rationale + `Settings.ACTION_USAGE_ACCESS_SETTINGS` deep link with polling-on-resume to detect grant (research.md §State Signal Collection)
- [x] T100 [P] [US7] Step 4: physical activity rationale + runtime request (`ACTIVITY_RECOGNITION`)
- [x] T101 [US7] Route `MAIN`/`LAUNCHER` to `OnboardingActivity` on first run (SharedPreferences flag `onboarding.completed=false`), else to `DiaryActivity`
- [x] T102 [US7] Write `PERMISSION_GRANTED`/`PERMISSION_REVOKED` audit rows for each permission outcome (contracts/audit-log-contract.md §3)
- [x] T103 [US7] Add "Grant later" escape on each step that marks `onboarding.completed=true` but keeps the missing-permission banner persistent in `DiaryScreen` until granted
- [x] T103a [US7] On permission-denied for `SYSTEM_ALERT_WINDOW` **or** `POST_NOTIFICATIONS` (both are structurally required to run the foreground capture service), surface a Compose rationale modal (NOT the system dialog — explains the consequence: "Without this, Orbit cannot capture. You can still browse the Diary in read-only mode."). On second decline, set `SharedPreferences.orbit.reducedMode = true`, skip `startForegroundService(CapsuleOverlayService)`, and route `MAIN`/`LAUNCHER` to `ReducedModeActivity` instead of `DiaryActivity`. Write one `PERMISSION_REDUCED_MODE_ENTERED` audit row. Per FR-021.
- [x] T103b [P] [US7] Create Compose `com.capsule.app.onboarding.ReducedModeActivity` — hosts `DiaryScreen` in read-only mode (no overlay, no new-capture FAB, no bubble service start); a persistent banner at the top reads "Capture is off. Tap to enable." → links to system permission settings. On resume, if both permissions are now granted, clear `reducedMode`, start `CapsuleOverlayService`, and hand off to `DiaryActivity`.

**Checkpoint**: Fresh install yields a 45-60s onboarding; declining optional permissions does not break core capture.

---

## Phase 10: Polish & Cross-Cutting Concerns

**Purpose**: Lock down privacy invariants, close the loop on self-dogfood, and run the quickstart as a release gate.

- [x] T104 [P] Add `SERVICE_STARTED` / `SERVICE_STOPPED` audit rows in `CapsuleOverlayService.onCreate`/`onDestroy` (contracts/audit-log-contract.md §3)
- [x] T105 [P] Add local-only counters in `com.capsule.app.audit.DebugDumpReceiver` — captures, continuation success rate, diary open count; broadcast-driven dump handler for `com.capsule.app.DEBUG_DUMP` (dev builds only) per plan.md §Source Code
- [x] T106 Gate `DebugDumpReceiver` behind `BuildConfig.DEBUG` so no production build exports counters
- [x] T107 [P] Rename user-visible strings "Capsule" → "Orbit" in `app/src/main/res/values/strings.xml` and any XML (`android:label`, `android:description`); leave package names untouched per plan.md (addresses pending `rename_references` todo for strings-only)
- [ ] T108 [P] Update launcher label + monochrome icon to "Orbit" in `res/drawable/`, `res/mipmap-anydpi-v26/ic_launcher.xml`
- [ ] T109 [P] Add `README.md` pointer at repo root linking to `.specify/memory/PRD.md`, `.specify/memory/constitution.md`, and `specs/002-intent-envelope-and-diary/quickstart.md`
- [ ] T110 Run quickstart.md §3..§4.7 end-to-end on a physical Pixel; record results in `specs/002-intent-envelope-and-diary/acceptance-results.md`
- [ ] T110a [P] Create `app/src/androidTest/java/com/capsule/app/regression/Spec001SmokeTest.kt` — regression harness covering the 001 primitives per FR-025 so 002's overlay/service changes cannot silently break capture: (a) bubble drag end position is clamped to the screen edge within the 200ms animation window; (b) `ClipboardFocusStateMachine.resetToIdle()` restores `FLAG_NOT_FOCUSABLE` after the capture sheet collapses (001 Clarification 2026-04-17); (c) service survives an OEM-simulated process kill via `Process.killProcess(Process.myPid())` and re-appears within the AlarmManager restart window; (d) `CapsuleOverlayService.onCreate` catches `ForegroundServiceStartNotAllowedException` on a simulated A15 restricted launch and no crash is logged. Fails the release gate if any 001 primitive regresses.
- [ ] T111 [P] MITM-proxy acceptance run (quickstart.md §6): assert zero outgoing HTTP from any process besides `:net`, no `Referer`, no cookies; attach `mitm.log` to acceptance doc
- [ ] T112 [P] Memory leak check on overlay + `:ml` unbind path using Android Studio Profiler over a 10-min capture burst (50 envelopes) — document results
- [ ] T113 [P] Performance validation: assert p50 seal < 200ms, p95 URL hydration < 30s, Diary p50 render < 1s per plan.md §Technical Context; adjust debouncers if violated
- [ ] T114 Archive the deleted `app/sampledata/specs/001-core-capture-overlay/*` artifacts (already deleted from 001 — confirm they are not referenced by any build target)

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Setup)**: no deps
- **Phase 2 (Foundational)**: depends on Phase 1 — **BLOCKS** all user stories
- **Phase 3 (US1 — Capture)**: depends on Phase 2
- **Phase 4 (US2 — Diary)**: depends on Phase 2; renders most usefully after Phase 3 but `DiaryScreen` can be developed against fake data from the start
- **Phase 5 (US3 — URL Continuation)**: depends on Phase 2; integrates with US1's seal and US2's EnvelopeCard
- **Phase 6 (US4 — Screenshots)**: depends on Phase 2; reuses US3's URL hydrate pipeline
- **Phase 7 (US5 — Context Labels)**: depends on Phase 2; enriches US2 cards — does not block Diary's initial render
- **Phase 8 (US6 — Audit)**: depends on Phase 2; completeness checked against US1/US3 writers
- **Phase 9 (US7 — Onboarding)**: depends on Phase 2; independent of other story phases
- **Phase 10 (Polish)**: depends on all P1 + P2 stories being complete

### Critical Path to MVP

Phase 1 → Phase 2 → Phase 3 (US1) → Phase 4 (US2) → Phase 5 (US3) → Phase 10 release gate. ~5–7 engineer-weeks solo.

### Parallel Opportunities

- Setup: T003, T004, T005, T006 all [P]
- Foundational: T011–T016, T018–T023, T026–T028 are [P] once T007–T010 are done
- US1: T029–T032 tests in parallel; T033–T037 impl in parallel; T038–T043 serialize on `OverlayViewModel` + `BubbleUI`
- US2: T044–T046 tests in parallel; T047–T051 impl in parallel; T052–T055 serialize on `DiaryActivity` / manifest
- US3: T056–T059 tests in parallel; T060–T062 + T064–T065 impl in parallel; T063 + T066 + T068 + T070 serialize on service wiring
- US4–US7 are largely independent of each other once Phase 2 is done and can be assigned to different contributors

---

## Parallel Example: User Story 3 (URL Continuation)

```bash
# Tests first — all in parallel
Task: "Contract test: NetworkGatewayContractTest"
Task: "Contract test: NoRefererNoCookiesTest"
Task: "Contract test: UidCheckTest"
Task: "Contract test: UrlHydrateWorkerTest"

# Core primitives — all in parallel
Task: "Implement SafeOkHttpClient in com.capsule.app.net"
Task: "Implement UrlValidator in com.capsule.app.net"
Task: "Implement ReadabilityExtractor in com.capsule.app.net"
Task: "Implement NanoSummariser in com.capsule.app.ai"
Task: "Author UrlSummaryPrompt in com.capsule.app.ai.prompts"

# Serialize the wiring after the above
Task: "Wire NetworkGatewayImpl into NetworkGatewayService"
Task: "Implement UrlHydrateWorker"
Task: "Wire ContinuationEngine.enqueueForNewEnvelope into seal()"
```

---

## Implementation Strategy

### MVP First (P1: US1 + US2 + US3)

1. Phase 1 → Phase 2 → Phase 3 (US1) → STOP + VALIDATE: undo + seal + audit visible in `adb`
2. → Phase 4 (US2) → STOP + VALIDATE: Diary renders today with real envelopes
3. → Phase 5 (US3) → STOP + VALIDATE: golden path from quickstart.md §3 works on charger + unmetered
4. Decide whether to ship MVP or include P2 stories before first dogfood

### Incremental Delivery (P2 stories, any order)

5. Phase 6 (US4 — Screenshots): biggest-bang-for-buck because screenshots dominate Android "graveyard"
6. Phase 7 (US5 — Context labels): narrative Diary feel
7. Phase 8 (US6 — Audit): trust surface, required before inviting external dogfood

### Polish Before Public

8. Phase 9 (US7 — Onboarding): raises time-to-trust for first-time installs
9. Phase 10: rename strings, MITM audit, perf regression run, quickstart acceptance

---

## Notes

- [P] tasks touch different files and have no blocking dependencies on incomplete tasks; verify before parallelising
- Every user-story task carries its story tag to keep traceability in PRs and commit messages
- All contract tests MUST fail before their implementation tasks; this is enforced by ordering (test task ID < impl task ID)
- Commit at each checkpoint; never across phase boundaries
- Do not modify `com.capsule.app.net.*` outside its package — the `OrbitNoHttpClientOutsideNet` lint rule (T003) will refuse the build
- Any new `AuditAction` requires constitution-compliant review (contracts/audit-log-contract.md §3)
- Keep AIDL parcels ≤ 8 KB per envelope / ≤ 256 KB per day page (contracts/envelope-repository-contract.md §7) — enforced by a test in T044
