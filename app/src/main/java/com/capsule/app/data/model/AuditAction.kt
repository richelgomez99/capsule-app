package com.capsule.app.data.model

/** Closed set of auditable actions for v1. */
enum class AuditAction {
    ENVELOPE_CREATED,
    ENVELOPE_ARCHIVED,
    ENVELOPE_DELETED,
    ENVELOPE_SOFT_DELETED,
    ENVELOPE_RESTORED,
    ENVELOPE_HARD_PURGED,
    INTENT_SUPERSEDED,
    INFERENCE_RUN,
    NETWORK_FETCH,
    CONTINUATION_SCHEDULED,
    CONTINUATION_COMPLETED,
    CAPTURE_SCRUBBED,
    URL_DEDUPE_HIT,
    PERMISSION_REDUCED_MODE_ENTERED
}
